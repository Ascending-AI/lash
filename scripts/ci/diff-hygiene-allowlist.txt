# One Git pathspec glob per line exempts matching added files from checks A and B.
# Remove entries when they stop matching tracked files; an immediately preceding
# "# keep: reason" comment preserves an entry intentionally reserved for future use.
# Bazel's generated module lock records the complete pinned dependency graph.
MODULE.bazel.lock

# The generated Test262 outcome record: `outcomes/**/*.tsv` shards, one per
# test directory cut as deep as the tree is hot (FIG-3646, FIG-3727),
# ratcheted by scripts/check_test262_ratchet.py.
crates/lash-typescript/tests/test262/outcomes/*.tsv
