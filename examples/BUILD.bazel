filegroup(
    name = "shared_rust_sources",
    srcs = glob(["shared/**/*.rs"]),
    visibility = ["//visibility:public"],
)

# The codemode parity cells are linked by
# `crates/lash-typescript/tests/codemode_parity_examples.rs` with `include_str!`,
# so they are compile inputs of that test target.
filegroup(
    name = "codemode_parity_cells",
    srcs = glob(["codemode-parity/*.ts"]),
    visibility = ["//visibility:public"],
)
