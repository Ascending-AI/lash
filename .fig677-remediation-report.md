# FIG-677 HARD remediation report

Tree remediated from `e00c8d19553c687049a64f052181ac20bb7918cd` on
`fig677-explicit-artifact-ownership`, then integrated by merging
`origin/main` at `be56acd6fe0fcf10648489eb4919f5a4b319a33f`. Both requested HARD scopes are
ready for bounded independent re-review, subject to the full-suite evidence
and bounded follow-up described below.

## Finding-to-fix and regression map

1. **Resumable Restate starts.** Post-registration workflow-submission and
   external-reference failures now retain staging inputs; cleanup happens only
   on authoritative abandonment. Deterministic injection covers registration,
   submission, external-ref failure, and recovery in
   `restate_start_failures_preserve_inputs_for_exact_recovery`.
2. **Production dependency wiring.** `LashCore` retains the host process-engine
   registry separately from session-scoped engines and passes the exact bound
   environment store/engines through facade, tool-intent, trigger, and session
   start paths. `host_owned_processes_run_without_application_session` and
   `session_trigger_process_visibility_conformance` exercise real controllers.
3. **Public replay.** Public session/facade starts carry the environment spec
   into the replayable effect and no longer republish through a retired staging
   owner. Existing process ownership is discovered/transferred idempotently.
   `process_start_and_cancel_emit_typed_observation_events` and the host-owned
   process test both repeat public starts after staging retirement.
4. **Recoverable process-prune cleanup.** `ProcessArtifactCleanup` is recorded
   transactionally with prune/tombstone state and acknowledged only after exact
   environment and engine-family release. Trait decorators forward the new
   APIs. In-memory, SQLite-reopen, PostgreSQL-reopen, and facade fault-injection
   tests preserve a shared owner and retry after each artifact family fails.
5. **Complete execution-owner cleanup.** Existing
   `effect_scope_retirements` rows now carry `artifact_cleanup_completed`.
   Session deletion plus immediate/deferred operation retirement enumerate
   pending scopes, permanently retire each configured artifact store, then ack.
   Immediate facade errors are returned. SQLite reopen injection fails once at
   each environment/module store; PostgreSQL/SQLite effect-journal tests prove
   pending/ack state, and the seven-test runtime-operation suite proves facade
   and deferred-sweep behavior.
6. **Real slow-writer evidence.** `ArtifactPublicationPause` is consumed inside
   memory, SQLite, and PostgreSQL publication before the serialized mutation;
   conformance starts a writer, observes the pause, commits retirement, resumes
   it, and requires typed refusal with no resurrected bytes/edge.
7. **PostgreSQL artifact serialization.** Every publish, retain, transfer,
   release, and retirement takes a stable advisory lock keyed by
   `(namespace, artifact_ref)` after ordered owner locks. Real PostgreSQL 16
   tests deterministically hold that exact key and prove a committed B owner
   survives A release, concurrent final releases remove bytes, and retirement
   fences a late publisher. The first race waits until `pg_stat_activity`
   proves release reached its serialization point, eliminating scheduler
   false-greens.
8. **Cross-store retirement recovery.** The same durable pending/ack evidence
   from item 5 survives reopen until every store succeeds. The recovery test
   additionally proves the authoritative journal fence immediately rejects a
   late runtime effect, and completed cleanup rejects late environment and
   module publication.

After integrating main's intervening generations, SQLite durable-core/process/
effect schemas are `58`/`34`/`19`, and PostgreSQL is `88`. PostgreSQL retains
the FIG-677 `86 -> 87` ownership migration while `87` is the supported migration
endpoint and `88` is the merged current generation. The committed PostgreSQL
shape, schema-congruence registry, retention census, and ADR 0093 are updated.
No compatibility, refcount, lease, heuristic, second lifecycle journal, or
FIG-2875 dependency was added.

## Current-main integration fix round

The merge produced seven conflicts. `schema-shape.txt`, `schema.sql`,
`lash-postgres-store/src/lib.rs`, and `postgres/schema/migrations.rs` retain
main's admitted effect-address, truthful-attribution, and pending-claim schema
changes together with FIG-677 exact artifact ownership; the combined PostgreSQL
generation is the next free value, `88`, with migration targets ending at `87`.
`tests/schema_drift.rs` derives current expectations from
`PostgresStorage::schema_version()`. `lash-restate/src/tests.rs` retains both
main's new effect identity/attribution imports and FIG-677 process environment
store/query coverage. `lash-sqlite-store/src/schema.rs` takes the next free
durable-core/process/effect generations (`58`/`34`/`19`) without a duplicate
version or compatibility path. Main's atomic session-graph append behavior was
accepted unchanged.

The exact required full command was run once after the merge:
`cargo test --workspace --all-targets --locked`. It reached the
`agent-workbench` binary and reported verbatim:

`test result: FAILED. 257 passed; 2 failed; 14 ignored; 0 measured; 0 filtered out; finished in 17.41s`

Both failures were current-branch integration failures, not pre-existing main
failures. `durable_process_registry_preserves_identity_lifecycle_and_fencing`
still expected immediate tombstone compaction; it now proves the two exact
pending artifact-cleanup identities, acknowledges both cleanups, and only then
expects compaction. `inject_message_scopes_emission_to_requested_session`
registered invented engine/environment references that the corrected trigger
path properly refuses; it now publishes a real Lashlang module and process
environment before registering. The facade exposes the ownership/helper types
required for that public integration setup. Exact focused reruns pass 1/1 each.
The full command was not repeated because this round explicitly bounded it to
one run; Clippy rebuilt every workspace target after these fixes.

## Green verification

All Cargo commands sourced `./env.sh` and used its derived target. PostgreSQL
fix-round commands used the owned PostgreSQL 16 container
`lash-fig677-fix1-pg16` at `127.0.0.1:33228` with
`LASH_REQUIRE_POSTGRES=1`.

Current-main fix-round gates:

- `cargo clippy --workspace --all-targets --locked -- -D warnings` — PASS.
- `cargo fmt --all -- --check` and `git diff --check` — PASS.
- `python3 scripts/check_version_bumps.py --base origin/main` — PASS, 48
  surfaces against merge-base `be56acd6fe0f` on the committed merge tree. The
  same check also passed against an unreachable staged-tree candidate before
  the merge commit was created.
- `python3 scripts/check_version_bump_fixtures.py` — PASS: component 88, floor
  50, 37 explicit migrations, 3 disjoint refusal kinds, 11 explicitly dropped
  post-floor indexes, 11 explicitly dropped post-floor columns, and 4 asserted
  checkpoints.
- Focused integration repairs:
  `cargo test -p agent-workbench --bin agent-workbench process_work_tests::durable_process_registry_preserves_identity_lifecycle_and_fencing --locked -- --exact`
  and
  `cargo test -p agent-workbench --bin agent-workbench tests::mail_payload_tests::inject_message_scopes_emission_to_requested_session --locked -- --exact`
  — PASS, 1/1 each.
- PostgreSQL 16:
  `cargo test -p lash-internal-postgres-store --test schema_drift --locked -- --nocapture`
  — PASS, 40/40;
  `... --test conformance artifact_races::postgres_ ...` — PASS, 3/3;
  `... --test conformance postgres_artifact_store_satisfies_conformance ...`,
  `... --test integration postgres_process_prune_cleanup_evidence_survives_reopen_when_configured ...`,
  `... retirement_removes_a_group_and_its_children_together ...`, and
  `... committed_shape_artifact_matches_the_ddl_artifact ...` — PASS, 1/1 each.
- `cargo test -p lash-runtime --test runtime_operation_retirement --features rlm --locked -- --nocapture`
  against that PostgreSQL 16 instance — PASS, 7/7.

The first direct invocation of `scripts/check_version_bumps.py` was refused by
the OS because the repository script is not executable; the successful bounded
invocation used `python3`, matching the repository's existing documented use.

- `cargo check --workspace --all-targets --locked` — PASS (final tree).
- `cargo clippy --workspace --all-targets --locked -- -D warnings` — PASS.
- `cargo fmt --all -- --check` — PASS.
- `git diff --check` — PASS.
- `python3 scripts/check_version_bumps.py --base "$(git merge-base origin/main HEAD)"`
  — PASS, 47 surfaces against merge-base `6eaa9eafe9ca`.
- `python3 scripts/check_version_bump_fixtures.py` — PASS: component 87,
  floor 50, 36 explicit historical migrations, and all recreation fixtures
  synchronized.
- `python3 scripts/check-postgres-json-carrier-coverage.py` — PASS: 25/25
  carriers classified.
- `bash scripts/check-production-file-size.sh` — PASS after extracting
  artifact-cleanup, retention, effect-controller-handle, and PostgreSQL
  retirement helpers into focused modules.
- `cargo test -p lash-sim --test schema_congruence --locked` — PASS, 10/10.
- `cargo test -p lash-sim --test retention_classification --locked` — PASS, 2/2.
- `cargo test -p lash-internal-postgres-store --test schema_drift --locked -- --nocapture`
  — PASS, 40/40 on PostgreSQL 16.
- `cargo test -p lash-internal-postgres-store committed_shape_artifact_matches_the_ddl_artifact --locked -- --nocapture`
  — PASS, 1/1.
- `cargo test -p lash-internal-sqlite-store every_database_publishes_the_version_its_open_enforces --locked`
  and `... public_session_schema_version_tracks_the_internal_schema_version ...`
  — PASS, 1/1 each.
- Focused process commands:
  `cargo test -p lash-internal-core prune_retains_exact_artifact_cleanup_until_acknowledged --locked -- --nocapture`,
  `cargo test -p lash-internal-restate restate_start_failures_preserve_inputs_for_exact_recovery --locked -- --nocapture`,
  `cargo test -p lash-runtime process_start_and_cancel_emit_typed_observation_events --locked -- --nocapture`,
  and the `lash-runtime --features rlm` filters
  `host_owned_processes_run_without_application_session`,
  `session_trigger_process_visibility_conformance`, and
  `process_prune_retries_each_artifact_release_after_registry_reopen` — PASS,
  1/1 each.
- Focused memory/SQLite commands:
  `cargo test -p lash-internal-lashlang in_memory_lashlang_artifact_store_satisfies_conformance --locked -- --nocapture`,
  `cargo test -p lash-internal-sqlite-store sqlite_artifact_store_satisfies_conformance --locked -- --nocapture`,
  `... scope_retirement_retries_each_artifact_store_after_reopen ...`,
  `... sqlite_prune_cleanup_evidence_survives_reopen_until_acknowledged ...`,
  and `... retirement_removes_a_group_and_its_children_together ...` — PASS,
  1/1 each.
- Focused PostgreSQL commands:
  `cargo test -p lash-internal-postgres-store --test conformance artifact_races::postgres_ --locked -- --nocapture`
  — PASS, 3/3;
  `... --test conformance postgres_artifact_store_satisfies_conformance --locked -- --nocapture`,
  `... --test integration postgres_process_prune_cleanup_evidence_survives_reopen_when_configured --locked -- --nocapture`,
  and `... retirement_removes_a_group_and_its_children_together --locked -- --nocapture`
  — PASS, 1/1 each.
- `cargo test -p lash-runtime --test runtime_operation_retirement --features rlm --locked -- --nocapture`
  — PASS, 7/7 across SQLite and PostgreSQL.

The required broad affected all-target check was also run at the start of the
remediation and passed; it was repeated once on that remediation tree because
later schema-congruence and compiler-directed Clippy fixes changed compiled
files. The current-main fix round's exact full-suite run and bounded repairs are
recorded above.

## Red-side negative controls

Every mutation below was applied alone, produced the intended oracle failure,
and was restored before the green runs.

1. Restored eager staging retirement on Restate scheduling failure, then ran
   `. ./env.sh && cargo test -p lash-internal-restate restate_start_failures_preserve_inputs_for_exact_recovery -- --nocapture`
   — FAILED: `a retriable workflow failure must not reclaim the committed process input`.
2. Passed the empty session engine registry to facade process administration,
   then ran `. ./env.sh && cargo test -p lash-runtime --features rlm host_owned_processes_run_without_application_session -- --nocapture`
   — FAILED: engine `lashlang` was not configured.
3. Restored prepublication under the public replay staging owner, then ran
   `. ./env.sh && cargo test -p lash-runtime process_start_and_cancel_emit_typed_observation_events -- --nocapture`
   — FAILED on replay: `artifact owner has been permanently retired`.
4. Removed pending/ack forwarding from the process-registry decorator, then ran
   `. ./env.sh && cargo test -p lash-runtime --features rlm process_prune_retries_each_artifact_release_after_registry_reopen -- --nocapture`
   — FAILED: prune incorrectly returned success while exact cleanup evidence
   and the injected failure remained untouched.
5. Changed SQLite pending-retirement selection from
   `artifact_cleanup_completed = 0` to `= 1`, then ran
   `. ./env.sh && cargo test -p lash-internal-sqlite-store scope_retirement_retries_each_artifact_store_after_reopen -- --nocapture`
   — FAILED before the injected artifact-store error because recovery enumerated
   no pending lifecycle evidence.
6. Removed the retired-owner check after the memory-store publication pause,
   then ran `. ./env.sh && cargo test -p lash-internal-lashlang in_memory_lashlang_artifact_store_satisfies_conformance -- --nocapture`
   — FAILED: `retirement must fence a late writer`.
7. Replaced PostgreSQL `lock_artifact` with a no-op, then ran
   `. ./env.sh && LASH_POSTGRES_DATABASE_URL=postgresql://postgres:lash@127.0.0.1:33228/lash LASH_REQUIRE_POSTGRES=1 cargo test -p lash-internal-postgres-store --test conformance postgres_artifact_release_observes_owner_that_commits_ahead_of_it -- --nocapture`
   — FAILED: `the owner that committed ahead of release must keep the bytes live`.

An initial version of negative control 7 only yielded once before releasing the
blocking transaction and false-greened under the no-op mutation. The persistent
test was tightened to observe the actual PostgreSQL wait state; the restored
lock then passed and the same no-op mutation failed deterministically.

## Risks, limits, and review readiness

- Findings 1-4, 6, and 7 are fully proven by direct deterministic regressions
  on every relevant path/backend named by the finding.
- Findings 5 and 8 are proven with one explicit backend-composition limit:
  injected fail/reopen at both artifact families uses a durable SQLite effect
  journal, durable SQLite module store, and in-memory environment store.
  PostgreSQL pending/ack durability, runtime sweeps, artifact conformance, and
  concurrency are proven separately on PostgreSQL 16; no PostgreSQL
  cross-store fault/reopen Cartesian duplicate was added.
- Current `origin/main` at `be56acd6fe0f` is merged rather than rebased, so the
  two previously reviewed FIG-677 commits remain intact. Seven semantic conflict
  resolutions and the two focused integration-test repairs require the requested
  bounded HARD re-review; the platform's final merge candidate remains the
  authoritative proof against any later main movement.
- The exact current-main full-suite aggregate is red as quoted above. Both
  failures were repaired and individually pass; the full suite was not rerun
  because this fix round required exactly one invocation. Clippy's all-target
  rebuild and the focused tests reduce, but do not erase, that explicit evidence
  limit.
- Six compiler-directed edits outside the ownership modules (four `Box::pin`
  sites and two private type aliases) are mechanical Clippy fallout from the
  larger async start future. Focused module extractions and historical
  version-bump fixture synchronization were also required by repository
  pre-commit gates; none changes protocol or lifecycle semantics.

Behavior/contracts re-review should focus on Restate resumability, host/facade/
trigger dependency identity, public replay, and cleanup error propagation.
Durability/concurrency/failure-path re-review should focus on transactionally
retained prune evidence, the reused scope-retirement pending/ack state machine,
stable PostgreSQL artifact locks and lock ordering, and the red/green schedules
above.

## Round 2: exhaustive workspace repair and repository-gate closure

Round 2 started from PR head `9309589a954decf58e6ce53a6e21996f145733e7`
after `git fetch origin`; the two untracked remediation specs remained untouched.
The pushed implementation commit for the repairs below is
`606ba99ce79b7e9e347607e8b6761f05f0458f58`; the following report-only commit
records that immutable SHA.

### Full-suite red-to-green map

The first real no-fail-fast run exposed the complete post-merge failure set:

`Summary [ 650.258s] 7776 tests run: 7720 passed, 51 failed, 5 timed out, 32 skipped`

Every failure maps to one of these root causes and repairs (line numbers name the
final implementation):

1. SQLite refusal and durable-read tests still encoded predecessor generations
   from before the merged FIG-677 schema changes. The exact current/predecessor
   values were updated in `crates/lash-sqlite-store/src/preflight/tests.rs:72`,
   `tests/durable_read_fixture.rs:106`, `tests/graph_sequence_cutover.rs:16`,
   `tests/process_event_time_cutover.rs:17`, `tests/conformance.rs:67`,
   `tests/conformance/pre_frame_key.rs:42`,
   `tests/conformance/schema_refusal.rs:39`, and
   `tests/storage_fixes.rs:376`; the four `fixtures/durable-read/v1/sqlite/*`
   artifacts were regenerated through the repository's ignored fixture writer.
   This accounts for the durable fixture, refusal, first-open, and public-version
   reds rather than widening any reader to accept both shapes.
2. Registry conformance tests pruned rows and then compacted tombstones without
   acknowledging the new exact cleanup evidence. The common drain in
   `crates/lash-conformance/src/conformance/helpers.rs:16` now completes the
   production pending/ack protocol, and the process-registry, trigger-retention,
   change-horizon, and state-machine suites call it before compaction. This fixes
   all memory/SQLite registry, retention, change-feed, facade-prune, and testing-
   conformance failures while keeping unacknowledged cleanup durable.
3. Trigger, worker, Restate, and facade tests used synthetic `ProcessInput::Engine`
   values without a real engine or execution-environment artifact. The shared
   identity-checked fixture store and engine are defined at
   `crates/lash-core/src/runtime/process/model/testing.rs:3` and
   `crates/lash-core/src/testing/mod.rs:66-157`, then wired into the failing
   router, FIG-1127, intent-law, worker-recovery, Restate replay/workflow/redrive,
   tool-ingress, and process-scope tests. These fixtures exercise production
   admission instead of bypassing it.
4. The two Lashlang forged-input tests accidentally asked the production store
   to publish malformed content, which correctly rejects it before the read-side
   oracle. `crates/lash-lashlang-runtime/src/lib_tests.rs:6` now delegates normal
   lifecycle operations and forges only `get_module_artifact`, preserving the
   claimed prepared-start validation boundary.
5. SQLite prune compared the candidate count with the wrong SQL statement's row
   count. `crates/lash-sqlite-store/src/process_registry_change.rs:211-231`
   records the tombstone `INSERT` count and rolls the transaction back on exact
   candidate/tombstone divergence.
6. The Restate scalar replay test permitted the original invocation to finish
   instead of crashing at the pending wait. At
   `crates/lash-restate/src/tests/process_registry_core.rs:1191` the first
   invocation is aborted at the seam, the resolution is installed, and replay
   permits only the new checkpoint while proving the scalar provider count,
   stable scalar/signal hashes, and signal bytes are unchanged.
7. A permanently retired deterministic process-start owner prevented a genuinely
   new start after authoritative prune. Local and Restate executors now distinguish
   that permanent fence from arbitrary store/engine errors at
   `crates/lash-core/src/runtime/effect/executor/process_local.rs:54-139` and
   `crates/lash-restate/src/controller/process_command.rs:47-134`: exact replay
   still transfers an existing edge, a retired staging owner causes post-register
   publication directly under the process owner, and every other publish/protect
   error returns before registration. The pre-existing error-injection oracle at
   `crates/lash/src/tests/tool_intent_ingress.rs:1450` proves no process is
   registered on an ordinary store failure; all memory, SQLite, and PostgreSQL
   trigger-delivery fence tests pass.
8. Three SQLite cold-helper tests failed only because the CI-required helper
   examples had not been built. Running the exact workflow prebuild
   `cargo build --workspace --locked --example sqlite-await-event-helper --example postgres-await-event-helper`
   made all three pass without changing manifests or helper discovery.
9. Round 4 withdrew the unmeasured scheduling explanation recorded here. The
   Restate body-deadline test is back at main's 30 ms response-start allowance;
   the final Round 4 evidence below is the retained claim.
10. Six independent named-contract minimizer fixtures were serialized inside one
    test and exceeded the unchanged 300 s nextest per-test ceiling under workspace
    contention. `crates/lash-sim/src/minimize.rs:1217-1275` preserves every fixture
    and assertion as six independent tests; focused evidence is 6/6 in 63.158 s.
    The facade UI and four fault-matrix timeouts were likewise proven individually
    without increasing or disabling the timeout.

The intermediate repair run was
`Summary [ 923.627s] 7776 tests run: 7762 passed, 9 failed, 5 timed out, 32 skipped`.
After the final two semantic corrections, the exact required command was green:

```text
$ . ./env.sh && cargo nextest run --workspace --all-targets --locked --no-fail-fast --profile ci --ignore-default-filter
Summary [ 536.295s] 7781 tests run: 7781 passed, 32 skipped
```

The increase from 7,776 to 7,781 is exactly the timeout-safe split of one six-
fixture minimizer test into six independently bounded tests; no case or assertion
was deleted, ignored, or weakened.

### Round 2 gate defects and negative controls

- Facade-only negative control: PR head 9309589a caused
  `python3 scripts/check_facade_only_examples.py` to name
  `examples/agent-workbench/src/main_sections/tests/mail_payload.rs:57,61,62,72`
  as direct `lashlang` imports. `crates/lash/src/rlm.rs:218` now exposes `parse`
  and `LinkedModule`; the example uses only `lash::rlm` and `lash::persistence`.
  Both the unit gate (5/5) and direct checker pass.
- PostgreSQL inventory negative control: PR head 9309589a made
  `test_repository_inventory_is_complete` report 25 discovered JSON carriers
  against an expected 24. `scripts/test_check_postgres_json_carrier_coverage.py:24`
  records 25 because FIG-677 added durable cleanup JSON; the suite passes 3/3.
- The exhaustive initial run above is the red-side evidence for every stale
  fixture, missing dependency, cleanup-ack, forged-read, replay, and prune-count
  oracle. The focused 5/5 and 6/6 reruns plus the final aggregate prove the
  repairs on the restored tree.

### Exact repository and static gates

- Every command in `.github/workflows/ci.yml`'s `Repository gates` step passed.
  The aggregate is recorded in `/tmp/fig677-round2-repository-gates-remainder.log`;
  the feature-coverage contract separately passed 29/29 and reported
  `59 declared features, 14 executable lanes, 0 explicit unresolved`.
- The earlier nested-admission-exemption explanation was not reproducible in
  Round 4 and is withdrawn. No `ORB_CARGO_ADMISSION` override is part of the
  retained verification evidence.
- Explicit gates passed:
  `python3 scripts/check_facade_only_examples.py`;
  `python3 scripts/check_version_bumps.py --base origin/main` (`48 surfaces
  against merge-base 1b2b8afc9feb`); and
  `python3 scripts/check_version_bump_fixtures.py` (`component 88`, floor 50,
  37 migrations, 3 refusal kinds, 11 dropped indexes, 11 dropped columns,
  4 checkpoints).
- `. ./env.sh && cargo clippy --workspace --all-targets --locked -- -D warnings`
  — PASS. The only diagnostic was a main-pre-existing test-only accessor;
  `crates/lash-core/src/runtime/turn_control.rs:1022` now compiles it only for
  crate tests or the existing `testing` feature, and no runtime caller changed.
- `. ./env.sh && cargo fmt --all -- --check` — PASS.
- `git diff --check` — PASS.
- `bash scripts/check-production-file-size.sh` — PASS after moving the
  test-only environment-store constructor into the adjacent testing module and
  compacting one test-support import; no production behavior moved.

### Round 2 risks and readiness

The executor's retired-owner recovery recognizes the exact error phrase already
emitted by all three owned artifact backends; arbitrary errors are explicitly
covered and fail closed before registration. Converting the older string-backed
store error APIs into a new public error variant would be a separate wire/API
cutover and is intentionally not mixed into FIG-677.

No obsolete ownerless API, compatibility path, refcount, heuristic abandonment,
second lifecycle journal, or independent artifact-kind discriminator was added.
The exhaustive workspace, repository, facade, version, Clippy, formatting, and
diff gates are green. Both bounded HARD re-review scopes are ready: behavior/
contracts should review the replay-versus-new-start branch and real fixture
wiring; durability/concurrency/failure paths should carry forward the Round 1
backend proofs and inspect the SQLite tombstone-count correction and cleanup ack.

## Round 3: current-main integration

The semantic integration merge is
`85b6cc8636f694c7f780ef90662eeed23604e931`, with parents Round 2
`85d42af861f542f816ec0f3ccec4dbbc4443cbb9` and current `origin/main`
`8d408a6fe73aaa97c6bd890b9d96957976d0b26e`. It was pushed to
`fig677-explicit-artifact-ownership`. GitHub CI run `34715936608` was queued for
that exact semantic head:
`https://github.com/Ascending-AI/lash/actions/runs/34715936608`.

### Conflict resolutions

- `crates/lash-core/src/lib.rs`: kept FIG-677 cleanup/owner exports and main's
  `ProcessEngineAdmission`/`ProcessEngineRegistration` exports at main's new
  placement; the removed `ProcessEngineValidationContext` was not restored.
- `crates/lash-core/src/runtime/mod.rs`: exported the same union from the
  runtime facade, retaining `publish_process_execution_env` and excluding
  main's superseded ownerless persistence helper.
- `crates/lash-core/src/runtime/process/testing/identity.rs`: retained the
  exact-owner publication refusal and used a test-only raw insert to exercise
  main's typed v4/v5 obsolete-family read refusals before proving v6.
- `crates/lash-core/src/runtime/session_manager/process_runners/control.rs`:
  kept FIG-677 replayable owned environment capture while adopting main's pure
  registration admission and identity stamp before journaling on both routes.
- `crates/lash-lashlang-runtime/src/lib.rs`: kept the four exact-owner artifact
  lifecycle operations on `ProcessEngine` and adopted main's separate pure
  Lashlang admission registration.
- `crates/lash/src/tests/processes_endstate.rs`: kept all Round 2 prune/reopen
  regressions and main's new worker-fault cases, updating the facade fixture to
  forward the exact-owner artifact-store contract.
- `fixtures/durable-read/v1/sqlite/durable-core.db`: took main's binary, then
  regenerated it from the merged code; no binary merge was attempted.
- `fixtures/durable-read/v1/sqlite/processes.db`: took main's binary, then
  regenerated it from the merged code; no binary merge was attempted.

Git's textual auto-merge had placed FIG-677's lifecycle defaults on main's new
`ProcessEngineRegistration`; `crates/lash-core/src/runtime/process/engine.rs`
was corrected to keep those methods on `ProcessEngine`, their actual owner.
Three stale test fixtures were moved from `.with_engine` to main's typed
registration seam. The commit hook's size check exposed two merged test files
over budget; `testing/test_protocol_fakes.rs` and
`tests/processes_endstate/recovery_dispositions.rs` are exact module
extractions with unchanged test bodies and assertions.

### Generations and fixture regeneration

- SQLite durable-core/process/effect: Round 2 `58/34/19`, main `57/33/18`,
  integrated `58/34/19`; no collision. Trigger remains `8`.
- PostgreSQL: Round 2 `88`, main `86`, integrated `88`; no collision.
- Process environment identity: Round 2 v5, main v6, integrated v6. Main owns
  this independent cutover; v4 and v5 remain typed refusals.
- Remote protocol: Round 2 `59`, main `60`, integrated `61`.
- Session-head metadata: Round 2 `9`, main `10`, integrated `11`.
- Durable-read fixture schema: Round 2 `63`, main `64`, integrated `65`.

The last three were silent concurrent-generation collisions detected by the
version gate. Each advanced exactly once to the next free value; no compatibility
decoder or dual path was added. Both backend fixtures were recaptured at fixture
generation 65 using:

```text
. ./env.sh && LASH_REGENERATE_DURABLE_READ_FIXTURES=1 cargo test -p lash-internal-sqlite-store --test durable_read_fixture regenerate_sqlite_durable_fixture --locked -- --ignored --exact
. ./env.sh && LASH_POSTGRES_DATABASE_URL=postgres://postgres:lash@127.0.0.1:33269/lash LASH_REGENERATE_DURABLE_READ_FIXTURES=1 cargo test -p lash-internal-postgres-store --test durable_read_fixture regenerate_postgres_durable_fixture --locked -- --ignored --exact
```

Both generators passed 1/1. The first SQLite attempt used the stale package ID
printed by one test diagnostic (`lash-sqlite-store`) and failed before running;
the repository's current manifest and module-level procedure name
`lash-internal-sqlite-store`, used successfully above.

### Focused verification

All Cargo commands sourced `./env.sh`; PostgreSQL tests used the owned
`postgres:16-alpine` container `lash-fig677-fix3-pg16` on port `33269`.

- `cargo check --workspace --all-targets --locked` — PASS on the final tree.
- `cargo clippy --workspace --all-targets --locked -- -D warnings` — PASS on
  the final tree.
- `cargo fmt --check` — PASS; `git diff --check` — PASS.
- `python3 scripts/check_version_bumps.py --base origin/main` — PASS, `48
  surfaces against merge-base 8d408a6fe73a` on the merge commit.
- Both durable-read consumers — PASS:
  `Summary [   0.730s] 2 tests run: 2 passed, 7845 skipped`.
- Entire `processes_endstate` module plus the conflict-touched environment,
  engine-admission, Lashlang four-shape, and process-runner route regressions —
  PASS: `Summary [   0.169s] 23 tests run: 23 passed, 7824 skipped`.
- The required pre-commit hooks, including the production/test file-size
  budget, passed on the merge commit.

The first Clippy run found four `large_futures` diagnostics in unchanged
current-main workbench approval tests. The mechanical `Box::pin` correction at
`examples/agent-workbench/src/main_sections/tests/approvals.rs:602` preserves
behavior; the exact all-target Clippy rerun passes. No full workspace test suite
was repeated in Round 3; CI is the full-suite leg as required.

### Round 3 risks and readiness

No Round 2 ownership, replay, cleanup, durability, or failure-path contract was
weakened. The only integration-only changes outside the named conflicts are
typed registration call-site updates, next-free generation increments,
deterministic fixture regeneration, a Clippy allocation at four test awaits,
and exact test-module extractions. Both bounded HARD re-review scopes can focus
on the engine/admission placement and the regenerated durable evidence while
carrying forward Round 2's exhaustive semantic proofs.

## Round 4: review correction and one-step catalog

Round 4 starts from merge head `bb3994a95b8363d8515c47b53a30256df916a5d8`,
whose second parent is `origin/main` `41ad16091392d04830644e58d4f5824c31654058`.
The exact-path Round 4 implementation commit is
`f2d2794c73015b4ff1616cfc1324bd66b21298b0`.

### A. Version-domain decisions

- Remote protocol: option (2). `crates/lash-remote-protocol/src/lib.rs` is back
  at 60. The branch-only 60-to-61 delta changed only the constant; FIG-677 adds
  no remote DTO or wire-shape field. The complete remote-protocol package plus
  both durable adjacency tests passed 128/128.
- Session-head metadata: option (2). `crates/lash-core/src/store/mod.rs` is back
  at 10. The branch-only 10-to-11 delta changed only the constant; FIG-677 adds
  no session-head field. The literal v9 predecessor refusal passed 1/1.
- Durable-read fixture schema: option (1). Generation 65 is required because
  FIG-677 changes the durable artifact-owner edge and cleanup-evidence shape.
  The immediate predecessor is the exact schema-64 expected JSON from
  `origin/main` at `41ad1609`, not data generated by this branch. Byte comparison
  passed with SQLite SHA-256
  `193c5b62ee865ce883f285e438d96202b2a03b5fd171718aec84df804d3494d7`
  and PostgreSQL SHA-256
  `51717687a104a97f8b19d9cfc095bda738ff4d69ec7659cbff8cb6e90f2de4e6`.
  The older schema-60 through schema-63 witnesses remain untouched.

### B. PostgreSQL catalog and fixtures

`origin/main` is component 86 and this branch is exactly component 87
(`crates/lash-postgres-store/src/lib.rs:328-334`). All 36 catalog rows target
86; no row targets 87, so 87 is refusal-only. The dead 86-to-87 statement row
and the false migration-floor declarations for the new owner tables are gone.
The fixture gate reports `component 87, floor 50, 36 explicit migrations, 3
disjoint refusal kinds, 10 explicitly dropped post-floor indexes, 11 explicitly
dropped post-floor columns, 4 asserted checkpoints`.

Both durable fixtures were regenerated with their documented `cargo test -p
... regenerate_* --locked -- --ignored --exact` commands, then regenerated
again; the full nine-file SHA-256 manifest was identical. The component-v1
refusal catalog was likewise regenerated twice with
`regenerate_postgres_prior_component_fixture_catalog`; its stable SHA-256 is
`534fb03a3d4ab1bc21a657312a90afa5d88a52c9c2ca20428f0f06f391167f15`.
Its authoring helper now recreates cleanup/owner tables from authoritative DDL,
stamps the current component dynamically, and drops the dependent cleanup table
before rebuilding the process registry (`durable_read_fixture.rs:190-310`).

### C1-C5 mechanisms and regressions

- C1: `ArtifactOwner::Process` carries `ProcessRef`, and its persisted owner key
  is `process_id@incarnation` (`model.rs:312-350`). Registration assigns the
  incarnation before local/Restate executors transfer or publish exact process
  ownership; prune reconstructs the retired incarnation from cleanup evidence
  (`process_admin.rs:690-720`).
  `stale_process_cleanup_cannot_release_reregistered_incarnation_owner` uses the
  production Lash facade, SQLite registry/store, a faulted first cleanup, the
  same reusable `ProcessId`, and a typed `ProcessIncarnationSuperseded` result.
- C2: SQLite always checks the durable artifact row before consulting the
  handle-local parsed-module cache and evicts a stale entry when the row is gone
  (`attachments.rs:487-519`).
  `sqlite_module_cache_does_not_resurrect_artifact_reclaimed_by_another_handle`
  opens two production handles, primes B, releases through A, and proves B
  returns `None`.
- C3: both conformance factories again prove the reopened store is a distinct
  handle with `Weak::ptr_eq` (`lash-conformance/.../artifact_store.rs:184-205`;
  `lashlang/.../conformance.rs:219-239`). The unmeasured 250 ms Restate
  response-start allowance is reverted to main's 30 ms at
  `lash-restate/src/tests/effect_execution.rs:493-499`; its exact focused test
  passed. The prior nested-admission-exemption explanation was not retained.
- C4: predecessor provenance and exact hashes are committed beside the schema-64
  witnesses. Current-branch generators are used only for current generation 65.
- C5: `postgres_process_cleanup_fault_reopens_retries_and_acknowledges_when_configured`
  (`artifact_cleanup_round4.rs:126-263`) uses the PostgreSQL registry and env
  store plus a separately faulted engine store. It proves durable pending state
  after the first-store success/second-store fault, drops and reconnects the
  PostgreSQL handles, retries both releases, preserves a shared owner, and only
  then acknowledges cleanup.

### Red-side negative controls

- Temporarily serializing `ArtifactOwner::Process` as bare `ProcessId`, then
  running the C1 regression with the workspace/all-target graph, produced:
  `Summary [   0.137s] 1 test run: 0 passed, 1 failed, 7868 skipped`; the failure
  was `acknowledging the stale cleanup must preserve the live incarnation's owner`.
- Temporarily restoring SQLite's pre-validation cache return, then running the
  C2 regression with the same graph, produced:
  `Summary [   0.409s] 1 test run: 0 passed, 1 failed, 7868 skipped`; the failure
  was `a handle-local cache must not resurrect durably reclaimed bytes`.
- Both mutations were restored before every green command and `git diff --check`.

### Exact final verification

Every Cargo command sourced `./env.sh`; PostgreSQL used the owned PostgreSQL 16
container on `127.0.0.1:33269` with `LASH_REQUIRE_POSTGRES=1`.

- New/focused production, conformance, replay, and restored 30 ms tests:
  `Summary [   7.877s] 9 tests run: 9 passed, 7860 skipped`.
- Remote protocol plus fixture adjacency: `Summary [   1.141s] 128 tests run:
  128 passed, 7741 skipped`; session-head predecessor: `Summary [   0.063s] 1
  test run: 1 passed, 7868 skipped`.
- Both durable-read consumers, including the nested component refusal:
  `Summary [   2.208s] 9 tests run: 9 passed, 3 skipped`.
- PostgreSQL schema-drift binary plus live committed-shape gate:
  `Summary [   8.296s] 41 tests run: 41 passed, 7828 skipped`.
- `. ./env.sh && cargo check --workspace --all-targets --locked` — PASS.
- `. ./env.sh && cargo clippy --workspace --all-targets --locked -- -D warnings`
  — PASS. Its first unchanged attempt exhausted the filesystem while this fork's
  regenerable `target/debug/incremental` occupied 168 GB; only that exact cache
  directory was cleared, and the unchanged rerun passed.
- The exact current `.github/workflows/ci.yml` Repository gates block — PASS,
  including feature coverage `31/31` and `59 declared features, 14 executable
  lanes, 0 explicit unresolved`. No admission override was used.
- `python3 scripts/check_facade_only_examples.py` — PASS;
  `python3 scripts/check_version_bumps.py --base origin/main` — PASS, 48 surfaces;
  `python3 scripts/check_version_bump_fixtures.py` — PASS;
  `python3 scripts/check-postgres-json-carrier-coverage.py` — PASS, 25 classified;
  `bash scripts/check-production-file-size.sh` — PASS;
  `. ./env.sh && cargo fmt --all -- --check` — PASS; `git diff --check` — PASS.

### Round 4 risks and readiness

The full workspace suite is intentionally the CI leg for this fix round. The
local evidence is claim-matched, uses the workspace feature graph except for the
repository's own package-scoped fixture-authoring commands, and includes a real
PostgreSQL cross-store reopen proof. The earlier unmeasured timeout and nested
admission claims are withdrawn. No shim, compatibility arm, refcount, second
journal, fixture widening, ignored regression, or FIG-2875 dependency was added.
Both HARD scopes are ready for bounded re-review: process-incarnation ownership
and executor transfer semantics; cache/durable cleanup failure and reopen paths.
