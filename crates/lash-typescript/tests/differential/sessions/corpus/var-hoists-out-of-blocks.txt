# One session of the Node session oracle's corpus (FIG-3599); the
# format and the cell mapping are README.md's, and ../generate.mjs
# answers this file's cells under ../expectations/var-hoists-out-of-blocks.json.

session var-hoists-out-of-blocks
about a var declared in a block or loop head of the top level is a session global
probe hoisted looped seen
cell
if (true) {
  var hoisted = "up";
}
const seen = [];
for (var looped of [1, 2]) {
  seen.push(looped);
}
console.log(hoisted, looped, seen);
end
