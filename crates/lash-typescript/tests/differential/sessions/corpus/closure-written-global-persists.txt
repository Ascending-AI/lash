# One session of the Node session oracle's corpus (FIG-3599); the
# format and the cell mapping are README.md's, and ../generate.mjs
# answers this file's cells under ../expectations/closure-written-global-persists.json.

session closure-written-global-persists
about a closure assigns a top-level binding, and the next cell reads the value it left, live and after a reload (FIG-3707)
probe n bump total by v
deviation closure-boundary
cell
let n = 0;
const bump = (by) => { n = n + by; };
bump(2);
bump(3);
console.log(n);
cell
let total = 0;
[1, 2, 3].forEach((v) => { total += v * n; });
console.log(n, total);
end
