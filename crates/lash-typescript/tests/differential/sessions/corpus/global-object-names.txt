# One session of the Node session oracle's corpus (FIG-3599); the
# format and the cell mapping are README.md's, and ../generate.mjs
# answers this file's cells under ../expectations/global-object-names.json.

session global-object-names
about a name written through globalThis is a session global a bare name reads
probe fresh old
cell
globalThis.fresh = 1;
var old = 1;
globalThis.old = 2;
console.log(fresh, old);
cell
console.log(fresh + old);
end
