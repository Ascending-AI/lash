# One session of the Node session oracle's corpus (FIG-3599); the
# format and the cell mapping are README.md's, and ../generate.mjs
# answers this file's cells under ../expectations/catch-and-switch-binders.json.

session catch-and-switch-binders
about a catch binder and a switch-case declaration end with their blocks
probe caught picked choice
cell
try {
  throw new Error("boom");
} catch (caught) {
  console.log(caught.message);
}
const choice = 2;
switch (choice) {
  case 2: {
    const picked = "two";
    console.log(picked);
    break;
  }
  default:
    console.log("other");
}
end
