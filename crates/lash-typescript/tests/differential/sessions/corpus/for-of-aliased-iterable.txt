# One session of the Node session oracle's corpus (FIG-3599); the
# format and the cell mapping are README.md's, and ../generate.mjs
# answers this file's cells under ../expectations/for-of-aliased-iterable.json.

session for-of-aliased-iterable
about a for...of loop visits what its body appends through an alias made before the loop (FIG-3625)
probe items same item
cell
const items = [1];
const same = items;
for (const item of items) {
  if (same.length < 3) {
    same.push(item + 1);
  }
}
console.log(same);
end
