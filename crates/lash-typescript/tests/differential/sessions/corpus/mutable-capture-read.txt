# One session of the Node session oracle's corpus (FIG-3599); the
# format and the cell mapping are README.md's, and ../generate.mjs
# answers this file's cells under ../expectations/mutable-capture-read.json.

session mutable-capture-read
about a closure made before its binding is reassigned reads the binding, not a copy (FIG-3707)
probe x f
deviation closure-boundary
cell
let x = 1;
const f = () => x;
x = 2;
console.log(f());
end
