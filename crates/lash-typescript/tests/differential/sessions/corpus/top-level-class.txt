# One session of the Node session oracle's corpus (FIG-3599); the
# format and the cell mapping are README.md's, and ../generate.mjs
# answers this file's cells under ../expectations/top-level-class.json.

session top-level-class
about class is a named rejection, so the cell never binds its name
probe Shape
cell reject TS_CLASS_UNSUPPORTED
class Shape {}
end
