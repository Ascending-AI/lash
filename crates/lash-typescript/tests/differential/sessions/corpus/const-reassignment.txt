# One session of the Node session oracle's corpus (FIG-3599); the
# format and the cell mapping are README.md's, and ../generate.mjs
# answers this file's cells under ../expectations/const-reassignment.json.

session const-reassignment
about assigning a const in the cell that declared it
probe fixed
cell reject TS_ASSIGN_CONST
const fixed = 1;
fixed = 2;
end
