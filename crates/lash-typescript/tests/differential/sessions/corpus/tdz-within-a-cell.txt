# One session of the Node session oracle's corpus (FIG-3599); the
# format and the cell mapping are README.md's, and ../generate.mjs
# answers this file's cells under ../expectations/tdz-within-a-cell.json.

session tdz-within-a-cell
about a read of a let before its declaration, which the dialect refuses statically
probe early before
cell reject TS_TEMPORAL_DEAD_ZONE
const before = 1;
console.log(typeof early);
let early = 2;
end
