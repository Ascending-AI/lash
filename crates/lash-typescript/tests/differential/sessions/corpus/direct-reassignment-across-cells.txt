# One session of the Node session oracle's corpus (FIG-3599); the
# format and the cell mapping are README.md's, and ../generate.mjs
# answers this file's cells under ../expectations/direct-reassignment-across-cells.json.

session direct-reassignment-across-cells
about assigning an earlier cell's let without declaring it, which the dialect refuses statically
probe tally
cell
let tally = 1;
cell reject TS_ASSIGN_CONST
tally = tally + 1;
console.log(tally);
end
