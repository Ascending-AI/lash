# One session of the Node session oracle's corpus (FIG-3599); the
# format and the cell mapping are README.md's, and ../generate.mjs
# answers this file's cells under ../expectations/var-in-an-if-arm.json.

session var-in-an-if-arm
about a var in an if arm is a session global
probe lifted
cell
if (true) {
  var lifted = "up";
}
console.log(lifted);
end
