# One session of the Node session oracle's corpus (FIG-3599); the
# format and the cell mapping are README.md's, and ../generate.mjs
# answers this file's cells under ../expectations/for-of-shadowed-iterable.json.

session for-of-shadowed-iterable
about a for...of body may declare a binding of its iterable's name (FIG-3625)
probe items item
cell
const items = [1, 2];
for (const item of items) {
  const items = [item];
  console.log(items.length);
}
end
