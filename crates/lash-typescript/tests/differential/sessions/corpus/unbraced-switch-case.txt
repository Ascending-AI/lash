# One session of the Node session oracle's corpus (FIG-3599); the
# format and the cell mapping are README.md's, and ../generate.mjs
# answers this file's cells under ../expectations/unbraced-switch-case.json.

session unbraced-switch-case
about a declaration directly in a switch case ends with the switch
probe label
cell
switch (1) {
  case 1:
    const label = "one";
    console.log(label);
    break;
}
end
