# One session of the Node session oracle's corpus (FIG-3599); the
# format and the cell mapping are README.md's, and ../generate.mjs
# answers this file's cells under ../expectations/builtin-method-call-spread.json.

session builtin-method-call-spread
about a spread argument to a builtin method passes the array's items, among fixed ones and nested (FIG-3627)
probe items rows count row
cell
const items = [3, 1];
items.push(...[4, 5]);
const rows = [[9, 2], [6]];
const count = items.push(0, ...rows.map((row) => Math.max(...row)));
console.log(items, count);
console.log(...items);
end
