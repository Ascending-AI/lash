# One session of the Node session oracle's corpus (FIG-3599); the
# format and the cell mapping are README.md's, and ../generate.mjs
# answers this file's cells under ../expectations/for-of-binder.json.

session for-of-binder
about a for...of binder ends with its loop (FIG-3571 phase-1 leak `item`)
probe items item total
cell
const items = [1, 2, 3];
let total = 0;
for (const item of items) {
  total = total + item;
}
console.log(total);
end
