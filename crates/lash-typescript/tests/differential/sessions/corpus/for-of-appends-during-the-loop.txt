# One session of the Node session oracle's corpus (FIG-3599); the
# format and the cell mapping are README.md's, and ../generate.mjs
# answers this file's cells under ../expectations/for-of-appends-during-the-loop.json.

session for-of-appends-during-the-loop
about a for...of loop visits what its body appends, and stops short of what it removes (FIG-3625)
probe queue seen item
cell
const queue = [1];
const seen = [];
for (const item of queue) {
  seen.push(item);
  if (item < 4) {
    queue.push(item * 2);
  }
}
console.log(seen, queue);
cell
for (const item of queue) {
  queue.pop();
  seen.push(item);
}
console.log(seen, queue);
end
