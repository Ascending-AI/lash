# One session of the Node session oracle's corpus (FIG-3599); the
# format and the cell mapping are README.md's, and ../generate.mjs
# answers this file's cells under ../expectations/var-loop-head.json.

session var-loop-head
about a var in a for...of head is the one var binding of the script, not a loop binding
probe shared
cell
var shared = 0;
for (var shared of [1, 2]) {
  console.log(shared);
}
console.log(shared);
end
