# One session of the Node session oracle's corpus (FIG-3599); the
# format and the cell mapping are README.md's, and ../generate.mjs
# answers this file's cells under ../expectations/runtime-error-classes.json.

session runtime-error-classes
about an uncaught error reports its class
probe value
cell
const value = null;
console.log(value.missing);
end
