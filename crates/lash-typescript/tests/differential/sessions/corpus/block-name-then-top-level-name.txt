# One session of the Node session oracle's corpus (FIG-3599); the
# format and the cell mapping are README.md's, and ../generate.mjs
# answers this file's cells under ../expectations/block-name-then-top-level-name.json.

session block-name-then-top-level-name
about a block binding and a later top-level binding of the same name are two bindings
probe first second
cell
{
  const first = 1;
  console.log(first);
}
let first = 2;
for (const second of [3]) {
  console.log(second);
}
const second = 4;
console.log(first, second);
end
