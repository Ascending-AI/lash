(() => { let x = 'outside'; let probe1, probe2; switch (null) { case null: let x = 'inside'; probe1 = () => x; case null: probe2 = () => x; } return `${probe1()}|${probe2()}|${x}`; })()
(() => { let x = 'outside'; let probe1, probe2; switch (0) { default: let x = 'inside'; probe1 = () => x; case 1: probe2 = () => x; } return `${probe1()}|${probe2()}|${x}`; })()
(() => { let acc = ''; switch (0) { case 0: acc += 'a'; break; acc += 'b'; case 1: acc += 'd'; } return acc; })()
(() => { let acc = ''; switch (0) { case 0: acc += 'a'; { break; acc += 'b'; } acc += 'c'; case 1: acc += 'd'; } return acc; })()
(() => { let acc = ''; switch (0) { case 0: acc += 'a'; if (true) { break; } acc += 'c'; case 1: acc += 'd'; } return acc; })()
(() => { const run = (value) => { let result = 0; switch (value) { case 0: switch (value) { case 0: result += 3; break; default: result += 32; break; } result *= 2; break; result = 3; default: result += 32; break; } return result; }; return run(0); })()
(() => { const map = { a: { aa: 1, ab: 2 }, b: { ba: 1 } }; let acc = ''; for (const key in map) { for (const inner in map[key]) { acc += '' + inner + map[key][inner]; } } return acc; })()
(() => { const a = { marker: 1 }; return [1].flatMap(function () { return [this]; }, a)[0] === a; })()
(() => [1].flatMap(function () { return [this]; }, 's')[0])()
(() => { const held = { v: 1 }; return [held].map(function () { return this.target; }, { target: held })[0] === held; })()
(() => { const seen = []; [1, 2].forEach(function (v) { seen.push(this.t * v); }, { t: 10 }); return seen.join(','); })()
(() => { const run = (value) => { let result = 0; switch (value) { case 0: try { result += 2; throw 'ex'; } finally { break; } default: result += 32; break; } return result; }; return run(0); })()
(() => { let acc = ''; for (let i = 0; i < 3; i++) { switch (i) { case 0: acc += 'a'; continue; case 1: try { acc += 'b'; throw 'x'; } finally { break; } case 2: acc += 'c'; continue; } acc += 'Z'; } return acc; })()
