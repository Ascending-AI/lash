(() => { try { const n = null; return n.x; } catch (e) { return `${e.name}|${e.message}`; } })()
(() => { try { const n = undefined; return n[0]; } catch (e) { return `${e.name}|${e.message}`; } })()
(() => { try { const n = null; n.x = 1; return 'wrote'; } catch (e) { return `${e.name}|${e.message}`; } })()
(() => { try { const f = 1; return f(); } catch (e) { return `${e.name}`; } })()
(() => { try { const {} = null; return 'destructured'; } catch (e) { return `${e.name}|${e.message}`; } })()
(() => { try { return (({}) => 1)(undefined); } catch (e) { return `${e.name}|${e.message}`; } })()
(() => { try { const [{ x }] = [null]; return x; } catch (e) { return `${e.name}`; } })()
(() => { try { return [].find(null); } catch (e) { return `${e.name}`; } })()
(() => { try { return [].findLastIndex(1); } catch (e) { return `${e.name}`; } })()
(() => { try { return Array.from([], null); } catch (e) { return `${e.name}`; } })()
Array.from([1, 2], undefined).length
(() => { try { return Object.groupBy([], {}); } catch (e) { return `${e.name}`; } })()
(() => { try { return JSON.parse('{'); } catch (e) { return `${e.name}`; } })()
(() => { try { return (1).toFixed(101); } catch (e) { return `${e.name}|${e.message}`; } })()
(() => { try { return 'a'.repeat(-1); } catch (e) { return `${e.name}|${e.message}`; } })()
(() => { try { return String.fromCodePoint(1.5); } catch (e) { return `${e.name}|${e.message}`; } })()
(() => { try { return 'a'.startsWith(/a/); } catch (e) { return `${e.name}|${e.message}`; } })()
(() => { try { return [].reduce((a, b) => a + b); } catch (e) { return `${e.name}|${e.message}`; } })()
(() => { try { return [1].with(5, 1); } catch (e) { return `${e.name}|${e.message}`; } })()
(() => { try { const a = []; a.length = -1; return a.length; } catch (e) { return `${e.name}|${e.message}`; } })()
(() => { try { return Object.assign(null); } catch (e) { return `${e.name}|${e.message}`; } })()
(() => { try { return Object.keys(undefined); } catch (e) { return `${e.name}|${e.message}`; } })()
(() => { try { return Error({ toString: undefined, valueOf: undefined }).message; } catch (e) { return `${e.name}|${e.message}`; } })()
(() => { try { return (function () {}).caller; } catch (e) { return `${e.name}|${e.message}`; } })()
(() => { try { const f = () => 1; f.arguments = 1; return 'wrote'; } catch (e) { return `${e.name}`; } })()
(() => { try { const m = new Map(); m.size = 1; return 'wrote'; } catch (e) { return `${e.name}|${e.message}`; } })()
(() => { try { const n = null; return delete n.x; } catch (e) { return `${e.name}|${e.message}`; } })()
((n) => { try { for (const x of n) { return x; } return 'none'; } catch (e) { return `${e.name}`; } })(5)
(() => { try { return Object.fromEntries([1]); } catch (e) { return `${e.name}|${e.message}`; } })()
JSON.stringify(Object.fromEntries([['a'], { 0: 'b', 1: 2 }]))
(() => { const r = ['a']; return JSON.stringify({ a: 1, b: 2 }, r); })()
JSON.stringify({ a: 1 }, 5)
JSON.stringify({ toJSON: null })
(() => { try { Math.PI = 3; return 'wrote'; } catch (e) { return `${e.name}`; } })()
