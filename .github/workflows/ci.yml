name: CI

# GitHub natively skips push/PR runs when the head commit requests a CI skip,
# so no per-job guards are needed for that.
#
# CI validates pull requests and the resulting commits on the main trunk.
# Releases are separate: a maintainer manually dispatches release.yml for a
# green commit on main. Ordinary merges never tag or publish.

on:
  # Manual full-profile validation serves release certification and recovery
  # when GitHub drops an automatic event (merge commit d70ce7ea got zero check
  # suites). It has no inputs, is never invoked from another workflow, and is
  # unrelated to the removed pass-2 bump chain.
  #
  # Don't drive this recovery by hand: `scripts/ci_ensure_run.sh <branch>
  # <head-sha>` waits a bounded window for the run GitHub should have made,
  # dispatches this trigger exactly once if none arrives, then polls that run
  # to a conclusion. A dispatch run is NOT equivalent to the run it replaces —
  # the event-conditional `Check versioned surface bumps` step below does not
  # run, and it builds refs/heads/<branch> rather than the PR merge tree — so
  # the script exits 3, not 0, on a green dispatch. Callers opt in with
  # --accept-dispatch-run. Release certification instead relies on the full
  # dispatch profile and the exact-SHA gate in release.yml.
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened, labeled]
  # The merge queue delivers a queued PR's required checks on its own event, from
  # its own `gh-readonly-queue/...` ref. Without this trigger a queue entry gets
  # no checks at all and the queue stalls.
  merge_group:
  # Trunk breadth, not a second copy of the queue run (FIG-2789). The queue
  # validates the required core board on its merge-group commit and merges main
  # by fast-forwarding to that same commit, so this push carries an identical
  # tree. Push skips that core board and keeps the breadth families — Test heavy
  # suites, Test S3 store, Functional E2E, Restate workers E2E, Fuzz smoke,
  # deferred Unicode — which are off the merge-queue critical path and gated to
  # `push`/`workflow_dispatch` only. Do not remove this trigger without first
  # giving the breadth families another home.
  push:
    branches:
      - main

# One live CI run per head branch, whatever event produced it — because a
# `pull_request` run carries `refs/pull/<n>/merge` while a manual recovery
# dispatch of the same commit carries `refs/heads/<branch>`, so keying on
# `github.ref` put them in different groups and ran the whole board twice over an
# identical tree. That duplication was the single largest line in the CI bill
# (54% of branch runner-minutes). A dispatch supersedes a pull-request run from
# the same branch instead of doubling it; the trigger supplies both the recovery
# path for a dropped event and the full profile required for release evidence.
#
# The key is not *only* the branch name, because a branch name is not unique
# across repositories: a fork's pull request from a branch literally named `main`
# would key into main's group and, being a pull-request run, would cancel a live
# trunk run — destroying the one guarantee below. So each event class that must
# never be collided into gets its own key, and those keys contain a colon, which
# `git check-ref-format` forbids in a ref name. That is what makes them
# unforgeable: no branch anyone can push, here or in a fork, can produce one.
#
# * `push` (main only) keys on `trunk:push`, so every newer automatic trunk run
#   cancels the older queued or active one. Cancellation is visible
#   supersession, not successful CI evidence; release.yml still requires its
#   own exact-target full-profile dispatch.
# * a dispatch on main keys on `trunk:dispatch`, so release certification for
#   the current main tip cannot cancel or be cancelled by automatic pushes. An
#   older main commit is certified from a temporary branch pinned to that exact
#   commit; its branch-named group is isolated from `trunk:push`. See the
#   release procedure in CONTRIBUTING.md.
# * a *fork* pull request keys on its PR number, which no branch name can
#   collide with. Same-repo pull requests keep the branch key, which is what lets
#   a dispatch dedupe with them.
# * a `merge_group` run has no `head_ref` and falls through to its own
#   `gh-readonly-queue/main/pr-<n>-<sha>` ref name, unique per queue entry: two
#   queue builds never share a group and never cancel each other. Their
#   cancellation belongs to the queue, which drops an entry's run when it
#   dequeues it, so `cancel-in-progress` stays false for the event.
concurrency:
  group: >-
    ${{ github.workflow }}-${{
      github.event_name == 'push' && 'trunk:push'
      || (github.event_name == 'workflow_dispatch' && github.ref_name == 'main') && 'trunk:dispatch'
      || (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository) && format('fork-pr:{0}', github.event.pull_request.number)
      || github.head_ref
      || github.ref_name
    }}
  cancel-in-progress: ${{ github.event_name == 'push' || github.event_name == 'pull_request' || (github.event_name == 'workflow_dispatch' && github.ref_name != 'main') }}

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  LASH_CI_FEATURES: ""
  # The linker is a property of the caches this workflow shares, not of any
  # one job's step list. RUSTFLAGS is part of cargo's per-unit fingerprint, so
  # a job that restores `linux-debug` under a different RUSTFLAGS than the job
  # that wrote it logs "cache restored" and then cold-builds the whole graph —
  # which is exactly what the five container/E2E readers did while the flag
  # was appended by a setup action they never ran. Setting it here makes
  # every job in this workflow resolve the same value by default; the one
  # cache that must stay mold-free (`linux-release`, restored by perf.yml and
  # release.yml, which do not link through mold) overrides it at job scope.
  # Mold reduces link cost across the workspace test binaries.
  RUSTFLAGS: -C link-arg=-fuse-ld=mold
  # Preserve the pre-worktree-isolation artifact tree consumed below.
  LASH_CONFIDENCE_OUT_DIR: ${{ github.workspace }}/target/confidence

jobs:
  # Keep path classification cheap and conservative. Every event whose exact
  # diff is unavailable fails open to all expensive families.
  plan:
    name: Plan CI
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    outputs:
      rust: ${{ steps.classify.outputs.rust }}
      stores: ${{ steps.classify.outputs.stores }}
      functional_e2e: ${{ steps.classify.outputs.functional_e2e }}
      workers_e2e: ${{ steps.classify.outputs.workers_e2e }}
      workbench: ${{ steps.classify.outputs.workbench }}
      regress: ${{ steps.classify.outputs.regress }}
      schema: ${{ steps.classify.outputs.schema }}
      rust_code: ${{ steps.classify.outputs.rust_code }}
      deps_config: ${{ steps.classify.outputs.deps_config }}
      docs_only: ${{ steps.classify.outputs.docs_only }}
      workflows_only: ${{ steps.classify.outputs.workflows_only }}
      e2e_relevant: ${{ steps.classify.outputs.e2e_relevant }}
      scripts_gates: ${{ steps.classify.outputs.scripts_gates }}
      fail_open: ${{ steps.classify.outputs.fail_open }}
      reason: ${{ steps.classify.outputs.reason }}
      postgres_matrix: ${{ steps.postgres-matrix.outputs.postgres_matrix }}
      bazel_trusted: >-
        ${{ github.event_name == 'merge_group'
        || github.event_name == 'push'
        || github.event_name == 'workflow_dispatch'
        || (github.event_name == 'pull_request'
        && github.actor != 'dependabot[bot]'
        && github.event.pull_request.head.repo.full_name == github.repository) }}

    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch --no-tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force FETCH_HEAD

      - name: Classify exact diff
        id: classify
        env:
          BEFORE_SHA: ${{ github.event.before }}
          CHECKOUT_TOKEN: ${{ github.token }}
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          MERGE_GROUP_BASE_SHA: ${{ github.event.merge_group.base_sha }}
        run: |
          set -euo pipefail

          fail_open() {
            python3 scripts/ci_plan.py fail-open --reason "$1" >> "${GITHUB_OUTPUT}"
            exit 0
          }

          case "${GITHUB_EVENT_NAME}" in
            workflow_dispatch)
              fail_open "workflow_dispatch has no exact triggering diff"
              ;;
            pull_request)
              base_sha="${PR_BASE_SHA}"
              ;;
            merge_group)
              base_sha="${MERGE_GROUP_BASE_SHA}"
              ;;
            push)
              base_sha="${BEFORE_SHA}"
              ;;
            *)
              fail_open "unsupported event ${GITHUB_EVENT_NAME}"
              ;;
          esac

          if [[ -z "${base_sha}" || "${base_sha}" == "0000000000000000000000000000000000000000" ]]; then
            fail_open "event has no exact base commit"
          fi
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          if ! git -c http.extraheader="AUTHORIZATION: basic ${auth_header}" \
            fetch --no-tags --prune --depth=1 origin "${base_sha}"; then
            fail_open "base commit ${base_sha} could not be fetched"
          fi
          if ! git rev-parse --verify "${base_sha}^{commit}" >/dev/null 2>&1; then
            fail_open "base commit ${base_sha} is unavailable"
          fi
          if ! git diff --name-status --no-renames -z "${base_sha}" "${GITHUB_SHA}" > "${RUNNER_TEMP}/changed-paths"; then
            fail_open "exact diff ${base_sha}..${GITHUB_SHA} could not be computed"
          fi
          python3 scripts/ci_plan.py classify \
            --paths-file "${RUNNER_TEMP}/changed-paths" >> "${GITHUB_OUTPUT}"

      # PG16 is the sole primary lane on rust pull requests and merge groups.
      # PG14/PG18 run when the diff touches a durable schema crate, or on
      # workflow_dispatch. Push skips the postgres job (queue already witnessed
      # the SHA). scripts/ci_plan.py owns the rule.
      - name: Resolve PostgreSQL matrix
        id: postgres-matrix
        run: |
          python3 scripts/ci_plan.py postgres-matrix \
            --event "${GITHUB_EVENT_NAME}" \
            --schema "${{ steps.classify.outputs.schema }}" >> "${GITHUB_OUTPUT}"

      - name: Summarize plan
        env:
          PLAN_JSON: ${{ toJson(steps.classify.outputs) }}
        run: |
          {
            echo "## CI plan"
            echo
            echo '```json'
            jq . <<< "${PLAN_JSON}"
            echo '```'
          } >> "${GITHUB_STEP_SUMMARY}"

  facade-only-examples:
    name: Facade-only examples
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    if: github.event_name != 'push'

    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --no-tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force FETCH_HEAD

      - name: Check example facade imports
        run: python3 scripts/check_facade_only_examples.py

  # Separate matrix jobs run the workspace check/doctests and API seal in
  # parallel. The matrix result remains the same required test-doc context.
  test-doc:
    name: ${{ matrix.name }}
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    needs: plan
    if: github.event_name != 'push'
    # Trusted events replace the workspace check and the workspace doctests
    # with cached Bazel actions, which need the shared cache credentials.
    # Untrusted events never see them and keep the Cargo commands below.
    environment: build-cache
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Check workspace + doctests
            lane: workspace
            target: target
          - name: Seal-test the API surface
            lane: seal
            target: target-seal
    env:
      # trybuild derives its fixture/output tree from Cargo's target directory.
      # The seal job has no shared writable output with any other CI job.
      CARGO_TARGET_DIR: ${{ github.workspace }}/${{ matrix.target }}

    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force FETCH_HEAD

      - name: Reclaim runner disk
        run: bash scripts/ci-reclaim-disk.sh

      - name: Install Rust toolchain
        uses: ./.github/actions/rust-toolchain


      - name: Restore cargo cache
        if: matrix.lane == 'workspace'
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          cache-bin: false
          shared-key: linux-debug

      - name: Restore isolated seal dependencies
        if: matrix.lane == 'seal'
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          cache-bin: false
          shared-key: linux-seal
          workspaces: . -> target-seal

      - name: Install protoc
        uses: ./.github/actions/install-tools
        with:
          tool: protoc

      - name: Install mold
        uses: ./.github/actions/setup-mold

      - name: Configure Bazel shared cache
        if: matrix.lane == 'workspace' && needs.plan.outputs.bazel_trusted == 'true'
        uses: ./.github/actions/bazel-shared-cache
        env:
          CACHE_ENDPOINT: ${{ secrets.CACHE_ENDPOINT }}
          CACHE_INSTANCE: ${{ secrets.CACHE_INSTANCE }}
          KILN_EXECUTOR_RUNTIME: ${{ secrets.KILN_EXECUTOR_RUNTIME }}
          CACHE_CA: ${{ secrets.CACHE_CA }}
          CACHE_CERT: ${{ secrets.CACHE_CERT }}
          CACHE_KEY: ${{ secrets.CACHE_KEY }}

      # `//:workspace_compile` is every first-party label of the resolved
      # default workspace graph except doctests: libraries, binaries, examples,
      # benches, build scripts, and the unit- and integration-test crates that
      # carry the `cfg(test)` shape. It compiles and links them, so it proves a
      # strict superset of `cargo check --workspace --all-targets`. The graph is
      # generated from the same `cargo metadata --locked` resolution the Cargo
      # command uses, so feature unification is identical and the members that
      # are not `default-members` — `lash-typescript` and `lashlang` — are
      # present. The one Cargo target outside the resolved graph,
      # `slack-clone-live-e2e`, is skipped by `cargo check` for the same reason.
      #
      # `//:workspace_doctests` is the same 35 rustdoc binaries Cargo builds,
      # run against the pinned toolchain and the declared dependency graph.
      - name: Check workspace and run doctests with shared cache
        if: matrix.lane == 'workspace' && needs.plan.outputs.bazel_trusted == 'true'
        shell: bash
        run: |
          # shellcheck disable=SC2086
          bazel --output_user_root="$RUNNER_TEMP/bazel-output" test \
            ${BAZEL_SHARED_CACHE_FLAGS} \
            --cache_test_results=yes --test_output=errors \
            //:workspace_compile //:workspace_doctests

      - name: Remove client credentials
        if: always() && matrix.lane == 'workspace'
        shell: bash
        run: rm -rf -- "$RUNNER_TEMP/build-cache"

      # Untrusted pull requests receive no cache credentials, so they keep
      # exactly the Cargo commands that predate the Bazel branch above.
      - name: Check workspace (all targets)
        if: matrix.lane == 'workspace' && needs.plan.outputs.bazel_trusted != 'true'
        run: |
          # shellcheck disable=SC2086
          cargo check --workspace --all-targets --locked ${LASH_CI_FEATURES}

      # nextest does not run doctests; keep the full workspace gate.
      - name: Test workspace doctests
        if: matrix.lane == 'workspace' && needs.plan.outputs.bazel_trusted != 'true'
        run: |
          # shellcheck disable=SC2086
          cargo test --doc --workspace --locked ${LASH_CI_FEATURES}

      # Every existing compile-fail/pass fixture remains in the ui binary.
      # Keep workspace feature unification and the original cargo test command.
      - name: Seal-test the API surface
        if: matrix.lane == 'seal'
        run: |
          # shellcheck disable=SC2086
          cargo test --workspace --locked ${LASH_CI_FEATURES} --test ui

      - name: Report cache disk capacity
        if: always()
        run: bash scripts/ci/report-cache.sh

  # Python-only repository gates. These were serial steps of test-doc, where
  # they sat behind twelve minutes of compilation for no reason: nothing here
  # depends on the workspace check.
  repo-gates:
    name: Repository gates
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    if: github.event_name != 'push'

    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force FETCH_HEAD

      - name: Install Rust toolchain
        uses: ./.github/actions/rust-toolchain

      - name: Install mold
        uses: ./.github/actions/setup-mold

      - name: Lint orchestrating tools
        run: python3 scripts/lint_orchestrating_tools.py

      - name: Check test quarantine metadata
        run: python3 scripts/check_test_quarantines.py

      - name: Check judged build geometry
        run: python3 scripts/check_judged_build_geometry.py

      - name: Test repository scripts
        run: |
          python3 scripts/test_agent_workbench_restate_e2e.py
          python3 scripts/test_bazel_test_contract.py
          python3 scripts/test_check_facade_only_examples.py
          python3 scripts/test_check_feature_coverage.py
          python3 scripts/check_feature_coverage.py check
          python3 scripts/test_check_postgres_json_carrier_coverage.py
          python3 scripts/test_check_postgres_payload_shape_version.py
          python3 scripts/test_check_service_gate_pinning.py
          python3 scripts/test_check_version_bumps.py
          python3 scripts/test_fast_test.py
          python3 scripts/test_check_version_bump_fixtures.py
          python3 scripts/test_check_transcript_diff.py
          python3 scripts/test_gate_scope.py
          python3 scripts/test_main_ci_policy.py
          python3 scripts/test_release_version.py
          python3 scripts/test_publish_workspace.py
          python3 scripts/test_production_file_size.py
          python3 scripts/test_confidence_gate_ci_contract.py
          python3 scripts/test_slack_clone_full_host_e2e_contract.py
          python3 scripts/test_slack_clone_live_model_e2e_contract.py
          python3 scripts/test_judged_runbook_matrix.py
          python3 scripts/test_check_judged_build_geometry.py
          python3 scripts/test_restate_workers_e2e_coverage.py
          python3 scripts/test_runtime_perf_percentiles.py
          python3 scripts/test_perfreport.py
          python3 scripts/test_ci_plan.py
          python3 scripts/test_ci_plan_hygiene.py
          python3 scripts/test_ci_workspace_cache.py
          python3 scripts/test_ci_artifact_provenance.py
          bash scripts/test-worktree-gate-env.sh
          bash scripts/test-agent-workbench-dev-reset.sh
          bash scripts/test-dev-script-process-identity.sh
          bash scripts/test-ci-reclaim-disk.sh

  # An unused dependency is a real cost: it enlarges the locked graph that
  # cargo-deny audits, the surface `Cargo.lock` churns on, and every build. The
  # check is source-level (cargo-shear parses the crates' `use` graph), so it is
  # cheap and needs no compiled artifacts.
  unused-deps:
    name: Unused dependencies
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    needs: plan
    if: github.event_name != 'push' && needs.plan.outputs.rust == 'true'

    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --no-tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force FETCH_HEAD

      - name: Install Rust toolchain
        uses: ./.github/actions/rust-toolchain

      # The workflow-level RUSTFLAGS link through mold, so even a host-tool
      # `cargo install` needs the linker present.
      - name: Install mold
        uses: ./.github/actions/setup-mold

      - name: Install cargo-shear
        run: cargo install cargo-shear --locked --version 1.13.4

      - name: Check for unused dependencies
        run: cargo shear

  # A dependency workspace's [patch] table is invisible to its consumers.
  # Resolve lashlang from this checkout as a Git dependency in a fresh external
  # crate so every dependency needed by downstream users is carried explicitly.
  lashlang-git-consumer:
    name: Lashlang Git consumer
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    needs: plan
    # Deferred on pull requests per the 2026-08-25 CI-scope ruling, but required
    # on every merge group -- including docs-only ones. This compile
    # configuration is resolved nowhere else, and the change classifier
    # certifies the diff, not the health of the base the group is built over
    # (FIG-2854).
    if: (github.event_name == 'merge_group' && (needs.plan.outputs.rust == 'true' || needs.plan.outputs.fail_open == 'true')) || (github.event_name == 'workflow_dispatch' && needs.plan.outputs.rust == 'true')

    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --no-tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force FETCH_HEAD

      - name: Reclaim runner disk
        run: bash scripts/ci-reclaim-disk.sh

      - name: Install Rust toolchain
        uses: ./.github/actions/rust-toolchain

      - name: Restore cargo cache
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          cache-bin: false
          shared-key: queue-lashlang-git-consumer
          workspaces: . -> target/lashlang-git-consumer
          save-if: ${{ github.event_name != 'merge_group' }}

      - name: Install mold
        uses: ./.github/actions/setup-mold

      - name: Check lashlang Git consumer without patch mirror
        run: bash scripts/check-lashlang-git-consumer.sh "${GITHUB_SHA}"

      - name: Report cache disk capacity
        if: always()
        run: bash scripts/ci/report-cache.sh

  # Feature-set coverage the workspace check cannot reach. The executable
  # registry owns every declared workspace feature, resolves ON/OFF graphs in
  # normal dependency context, and runs the exact package checks for this lane.
  # Split out of test-doc: these resolve their own feature graphs, so they
  # neither reuse nor warm anything the workspace check produces.
  package-feature-checks:
    name: Package feature check (${{ matrix.lane }})
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    needs: plan
    # Deferred on pull requests per the 2026-08-25 CI-scope ruling, but required
    # on every merge group -- including docs-only ones. Every matrix leg keeps
    # one exact package feature graph independently visible and cacheable.
    if: (github.event_name == 'merge_group' && (needs.plan.outputs.rust == 'true' || needs.plan.outputs.fail_open == 'true')) || (github.event_name == 'workflow_dispatch' && needs.plan.outputs.rust == 'true')
    strategy:
      fail-fast: false
      matrix:
        include:
          - lane: sansio-schema-validation
            command: python3 scripts/check_feature_coverage.py run sansio-schema-validation
          - lane: otel-feature-chain
            command: python3 scripts/check_feature_coverage.py run otel-feature-chain
          - lane: core-internal-features
            command: python3 scripts/check_feature_coverage.py run core-internal-features
          - lane: language-testing-features
            command: python3 scripts/check_feature_coverage.py run language-testing-features
          - lane: llm-transport-features
            command: python3 scripts/check_feature_coverage.py run llm-transport-features
          - lane: store-features
            command: python3 scripts/check_feature_coverage.py run store-features
          - lane: runtime-features
            command: python3 scripts/check_feature_coverage.py run runtime-features
          - lane: protocol-rlm-testing
            command: python3 scripts/check_feature_coverage.py run protocol-rlm-testing
          - lane: remote-protocol-conversions
            command: python3 scripts/check_feature_coverage.py run remote-protocol-conversions
          - lane: tool-lashlang-proxies
            command: python3 scripts/check_feature_coverage.py run tool-lashlang-proxies
          - lane: provider-testing-features
            command: python3 scripts/check_feature_coverage.py run provider-testing-features
          - lane: perf-dhat-heap
            command: python3 scripts/check_feature_coverage.py run perf-dhat-heap
          - lane: regress-stable-features
            command: python3 scripts/check_feature_coverage.py run regress-stable-features
          - lane: host-features
            command: python3 scripts/check_feature_coverage.py run host-features

    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --no-tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force FETCH_HEAD

      - name: Reclaim runner disk
        run: bash scripts/ci-reclaim-disk.sh

      - name: Install Rust toolchain
        uses: ./.github/actions/rust-toolchain

      # Main/dispatch runs write dependency outputs for this exact graph. Queue
      # refs restore the compatible main cache but never publish ephemeral
      # queue-scoped entries.
      - name: Restore package feature cache
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          cache-bin: false
          shared-key: queue-package-${{ matrix.lane }}
          save-if: ${{ github.event_name != 'merge_group' }}

      - name: Install protoc
        uses: ./.github/actions/install-tools
        with:
          tool: protoc

      - name: Install mold
        uses: ./.github/actions/setup-mold

      - name: Run exact package feature graph
        run: ${{ matrix.command }}

      - name: Report cache disk capacity
        if: always()
        run: bash scripts/ci/report-cache.sh

  # The default-off boundary lane: lash-runtime without its default features
  # compiles, tests, and pulls no RLM/Lashlang dependency.
  runtime-feature-boundary:
    name: Runtime feature boundary (${{ matrix.lane }})
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    needs: plan
    # Deferred on pull requests per the 2026-08-25 CI-scope ruling, but required
    # on every merge group -- including docs-only ones. The independent feature
    # resolutions run concurrently; fail-fast stays off so one failure never
    # suppresses evidence from another required graph.
    if: (github.event_name == 'merge_group' && (needs.plan.outputs.rust == 'true' || needs.plan.outputs.fail_open == 'true')) || (github.event_name == 'workflow_dispatch' && needs.plan.outputs.rust == 'true')
    strategy:
      fail-fast: false
      matrix:
        include:
          - lane: default-off-check
            command: cargo check -p lash-runtime --no-default-features --locked
          - lane: testing-check
            command: cargo check -p lash-runtime --no-default-features --features testing --locked
          - lane: default-off-tests
            command: |
              cargo test -p lash-runtime --no-default-features --locked
              count=$(cargo test -p lash-runtime --no-default-features --locked --lib -- --list | grep -c ': test$')
              [ "$count" -ge 130 ] || { echo "default-build lash-runtime tests regressed: $count"; exit 1; }
          - lane: dependency-boundary
            command: |
              if cargo tree -p lash-runtime -e normal --no-default-features --locked | grep -E 'lash-internal-protocol-rlm|lash-internal-lashlang-runtime|lash-internal-lashlang'; then
                echo "default-off lash-runtime pulled RLM/Lashlang dependencies"
                exit 1
              fi
              if cargo tree -p lash-runtime -e normal --locked | grep -E 'lash-internal-protocol-rlm|lash-internal-lashlang-runtime|lash-internal-lashlang'; then
                echo "default lash-runtime pulled RLM/Lashlang dependencies"
                exit 1
              fi
              if cargo tree -p lash-runtime -e normal --no-default-features --features testing --locked | grep -E 'lash-internal-protocol-rlm|lash-internal-lashlang-runtime|lash-internal-lashlang'; then
                echo "testing-only lash-runtime pulled RLM/Lashlang dependencies"
                exit 1
              fi

    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --no-tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force FETCH_HEAD

      - name: Reclaim runner disk
        run: bash scripts/ci-reclaim-disk.sh

      - name: Install Rust toolchain
        uses: ./.github/actions/rust-toolchain

      - name: Restore runtime feature cache
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          cache-bin: false
          shared-key: queue-runtime-${{ matrix.lane }}
          save-if: ${{ github.event_name != 'merge_group' }}

      - name: Install protoc
        uses: ./.github/actions/install-tools
        with:
          tool: protoc

      - name: Install mold
        uses: ./.github/actions/setup-mold

      - name: Run exact runtime feature graph
        env:
          FEATURE_GRAPH_COMMAND: ${{ matrix.command }}
        run: bash -euo pipefail -c "${FEATURE_GRAPH_COMMAND}"

      - name: Report cache disk capacity
        if: always()
        run: bash scripts/ci/report-cache.sh
  # The deterministic/default-feature partition is authoritative on trusted
  # events. The job executes locally on a GitHub-hosted runner and only shares
  # action results with that exact runner image identity, never Kiln's executor
  # identity. Fork and Dependabot pull requests intentionally skip this
  # secret-bearing job and retain the full Cargo workspace suite below.
  bazel-tests:
    name: Test Bazel partition
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    needs: plan
    if: github.event_name != 'push' && needs.plan.outputs.bazel_trusted == 'true' && needs.plan.outputs.rust == 'true'
    environment: build-cache
    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch --no-tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force FETCH_HEAD

      - name: Configure shared build cache
        uses: ./.github/actions/bazel-shared-cache
        env:
          CACHE_ENDPOINT: ${{ secrets.CACHE_ENDPOINT }}
          CACHE_INSTANCE: ${{ secrets.CACHE_INSTANCE }}
          KILN_EXECUTOR_RUNTIME: ${{ secrets.KILN_EXECUTOR_RUNTIME }}
          CACHE_CA: ${{ secrets.CACHE_CA }}
          CACHE_CERT: ${{ secrets.CACHE_CERT }}
          CACHE_KEY: ${{ secrets.CACHE_KEY }}

      # Every label here is deterministic and service-free, so reusing a
      # successful result is sound. The service-backed labels are `manual` and
      # never reach this aggregate; scripts/ci/store-tests.sh runs those with
      # `--nocache_test_results`.
      - name: Test deterministic workspace suite with shared cache
        shell: bash
        run: |
          # shellcheck disable=SC2086
          bazel --output_user_root="$BAZEL_OUTPUT_USER_ROOT" test \
            ${BAZEL_SHARED_CACHE_FLAGS} \
            --cache_test_results=yes --test_output=errors \
            //:workspace_tests

      - name: Remove client credentials
        if: always()
        shell: bash
        run: rm -rf -- "$RUNNER_TEMP/build-cache"

  # Build and execute the Cargo-owned partition on one runner. Its generated
  # nextest filter is intersected with profile.ci, preserving ordinary cases
  # in the Cargo-owned PR filter (service-gated, trybuild, and workbench
  # binaries are excluded so they are not compiled just to self-skip).
  # Untrusted pull requests receive no cache credentials, so they omit the
  # filter and retain the full workspace coverage that predates this cutover.
  workspace-tests:
    name: Test Cargo workspace partition
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    needs: plan
    if: github.event_name != 'push' && (needs.plan.outputs.rust == 'true' || needs.plan.outputs.workbench == 'true')
    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git config gc.auto 0
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --no-tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force "${GITHUB_SHA}"

      - name: Reclaim runner disk
        run: bash scripts/ci-reclaim-disk.sh

      - name: Install Rust toolchain
        uses: ./.github/actions/rust-toolchain

      - name: Install Node for browser projection gates
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24

      - name: Report runner capacity
        run: |
          echo "Logical CPUs: $(nproc)"
          free -h
          df -h /

      - name: Resolve workspace test cache inputs
        id: workspace-test-cache-inputs
        run: |
          compiler_digest="$(rustc -vV | sha256sum | cut -d ' ' -f 1)"
          settings_digest="$(printf '%s\0%s\0' "${RUSTFLAGS}" "${LASH_CI_FEATURES}" | sha256sum | cut -d ' ' -f 1)"
          source_digest="$(python3 scripts/ci_workspace_cache.py digest)"
          {
            echo "compiler=${compiler_digest}"
            echo "settings=${settings_digest}"
            echo "source=${source_digest}"
          } >> "${GITHUB_OUTPUT}"

      - name: Restore cargo cache
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          cache-bin: false
          shared-key: linux-tests
          # Workspace outputs have their own source-aware cache below.
          cache-targets: false

      # Keep the primary key source-aware so an unchanged tree gets an exact
      # hit. The compatibility prefix deliberately stops before the source
      # hash: Cargo validates restored fingerprints and incrementally rebuilds
      # a changed crate, while dependency/toolchain changes cannot cross it.
      # Only build-bearing target subtrees are retained, and cache service
      # failures never replace the test result.
      - name: Restore workspace test build outputs
        id: workspace-test-cache
        continue-on-error: true
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            target/debug/.fingerprint
            target/debug/build
            target/debug/deps
            target/debug/examples
            target/workspace-test-cache/source-mtimes.json
          key: >-
            workspace-tests-v2-${{ runner.os }}-${{ runner.arch }}-${{
            steps.workspace-test-cache-inputs.outputs.compiler }}-${{
            steps.workspace-test-cache-inputs.outputs.settings }}-${{
            hashFiles('Cargo.lock', 'Cargo.toml', '**/Cargo.toml', '.cargo/**', 'rust-toolchain*', '.github/actions/rust-toolchain/**', '.github/actions/setup-mold/**', '.github/actions/install-tools/**')
            }}-${{ steps.workspace-test-cache-inputs.outputs.source }}
          restore-keys: |
            workspace-tests-v2-${{ runner.os }}-${{ runner.arch }}-${{ steps.workspace-test-cache-inputs.outputs.compiler }}-${{ steps.workspace-test-cache-inputs.outputs.settings }}-${{ hashFiles('Cargo.lock', 'Cargo.toml', '**/Cargo.toml', '.cargo/**', 'rust-toolchain*', '.github/actions/rust-toolchain/**', '.github/actions/setup-mold/**', '.github/actions/install-tools/**') }}-

      - name: Restore unchanged source mtimes
        continue-on-error: true
        run: python3 scripts/ci_workspace_cache.py restore target/workspace-test-cache/source-mtimes.json

      - name: Snapshot source mtimes
        run: python3 scripts/ci_workspace_cache.py snapshot target/workspace-test-cache/source-mtimes.json

      - name: Install protoc and nextest
        uses: ./.github/actions/install-tools
        with:
          tool: protoc,nextest

      - name: Install mold
        uses: ./.github/actions/setup-mold

      # Trusted events select the generated Cargo-owned binary partition;
      # untrusted pull requests omit the filter and retain full-workspace
      # coverage. In either mode profile.ci excludes the multi-minute heavy
      # cases run by heavy-tests below and the trybuild ui binary run by
      # test-doc's seal step. Keep the recovery helpers in this target because
      # their certification fixtures are dev-only.
      - name: Test workspace
        env:
          BAZEL_TRUSTED: ${{ needs.plan.outputs.bazel_trusted }}
        run: |
          rust="${{ needs.plan.outputs.rust }}"
          workbench="${{ needs.plan.outputs.workbench }}"
          case "${BAZEL_TRUSTED}" in
            true)
              terms=()
              if [ "${rust}" = "true" ]; then
                terms+=("$(tr -d '\n' < tools/bazel/cargo_owned_nextest_filter.txt)")
              fi
              if [ "${workbench}" = "true" ]; then
                terms+=("$(tr -d '\n' < tools/bazel/workbench_nextest_filter.txt)")
              fi
              if [ "${#terms[@]}" -eq 0 ]; then
                echo "cargo partition selected with neither rust nor workbench" >&2
                exit 1
              fi
              filter="${terms[0]}"
              for term in "${terms[@]:1}"; do
                filter="(${filter}) + (${term})"
              done
              nextest_args=(-E "${filter}")
              ;;
            false)
              skip='not (package(lash-internal-postgres-store) + package(lash-internal-s3-store) + test(cross_backend) + (package(lash-runtime) & binary(ui)))'
              if [ "${workbench}" != "true" ]; then
                skip="${skip} & not package(agent-workbench)"
              fi
              nextest_args=(-E "${skip}")
              ;;
            *)
              echo "invalid Bazel trust decision: ${BAZEL_TRUSTED}" >&2
              exit 1
              ;;
          esac
          excludes=(--exclude lash-internal-postgres-store --exclude lash-internal-s3-store)
          if [ "${workbench}" != "true" ]; then
            excludes+=(--exclude agent-workbench)
          fi
          # shellcheck disable=SC2086
          cargo build --workspace --locked ${LASH_CI_FEATURES} \
            "${excludes[@]}" \
            --example sqlite-await-event-helper
          # shellcheck disable=SC2086
          cargo nextest run --profile ci --workspace --locked ${LASH_CI_FEATURES} \
            --no-fail-fast "${nextest_args[@]}"

      - name: Save workspace test build outputs
        if: success() && steps.workspace-test-cache.outputs.cache-hit != 'true'
        continue-on-error: true
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            target/debug/.fingerprint
            target/debug/build
            target/debug/deps
            target/debug/examples
            target/workspace-test-cache/source-mtimes.json
          key: >-
            workspace-tests-v2-${{ runner.os }}-${{ runner.arch }}-${{
            steps.workspace-test-cache-inputs.outputs.compiler }}-${{
            steps.workspace-test-cache-inputs.outputs.settings }}-${{
            hashFiles('Cargo.lock', 'Cargo.toml', '**/Cargo.toml', '.cargo/**', 'rust-toolchain*', '.github/actions/rust-toolchain/**', '.github/actions/setup-mold/**', '.github/actions/install-tools/**')
            }}-${{ steps.workspace-test-cache-inputs.outputs.source }}

      - name: Report cache disk capacity
        if: always()
        run: bash scripts/ci/report-cache.sh

  # The heavy suites excluded from the per-push workspace job by `profile.ci`'s
  # default-filter (see .config/nextest.toml): the fault-matrix chunk tests,
  # which each fork real `cargo` invocations and run 3-9 minutes serially, and
  # the lash-sim runner/minimizer fixture cases. Deferred to trunk runs
  # (push/dispatch) like the other trunk-only jobs: any one of these tests set
  # the wall-clock of the ordinary suite, and their behavior almost never varies
  # with a PR's diff.
  heavy-tests:
    name: Test heavy suites
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    needs: plan
    if: github.event_name != 'pull_request' && github.event_name != 'merge_group' && needs.plan.outputs.rust == 'true'

    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --no-tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force FETCH_HEAD

      - name: Reclaim runner disk
        run: bash scripts/ci-reclaim-disk.sh

      - name: Install Rust toolchain
        uses: ./.github/actions/rust-toolchain

      - name: Install Node for browser projection gates
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24

      - name: Restore cargo cache
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          cache-bin: false
          shared-key: linux-debug
          save-if: false

      - name: Install protoc and nextest
        uses: ./.github/actions/install-tools
        with:
          tool: protoc,nextest

      - name: Install mold
        uses: ./.github/actions/setup-mold

      - name: Test heavy suites
        run: |
          # shellcheck disable=SC2086
          cargo nextest run --profile ci-heavy --workspace --locked ${LASH_CI_FEATURES} \
            --no-fail-fast

      - name: Report cache disk capacity
        if: always()
        run: bash scripts/ci/report-cache.sh

  lint:
    name: Lint
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    needs: plan
    if: github.event_name != 'push'
    # Trusted events run the workspace clippy as cached Bazel actions, which
    # need the shared cache credentials. Untrusted events never see them and
    # keep the Cargo command below.
    environment: build-cache
    # The transcript gate reads the `Transcript:` justification from the PR
    # body. A manually dispatched run — the recovery path when GitHub stops
    # delivering a branch's events — has no pull request in its payload, so the
    # gate asks the API for it instead. Job-level and read-only: no other job
    # needs it.
    permissions:
      contents: read
      pull-requests: read

    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --no-tags --prune origin "+refs/heads/*:refs/remotes/origin/*"
          git fetch \
            --force --tags origin
          git fetch \
            --no-tags origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force FETCH_HEAD

      # Every event compares against the base its own tree actually sits on.
      #
      # PRs compare with their merge-base against main, not their target
      # branch: main is the sole long-lived branch, and a stacked PR whose
      # target is a sibling layer would otherwise be judged against a tree
      # that already carries the window's one version bump. Main pushes compare
      # with HEAD~1 so the landed tree is independently rechecked; manual
      # workflow dispatches have no meaningful base and intentionally skip it.
      # A merge-queue entry is neither: its head is a queue commit carrying one
      # or more PRs on top of the base the queue chose, so HEAD~1 would check
      # only the last commit of a multi-commit PR — passing vacuously when the
      # change is earlier, and failing falsely when the bump is earlier. The
      # queue states the base it built on, so use it.
      # The PR guarantee requires a current merge ref, enforced by an up-to-date
      # branch rule or merge queue; a stale merge ref has a stale merge-base.
      # Manual recovery runs skip this gate and immediately emit a warning summary
      # so their green conclusion cannot be mistaken for version-surface proof.
      - name: Check versioned surface bumps
        if: github.event_name != 'workflow_dispatch'
        env:
          MERGE_GROUP_BASE_SHA: ${{ github.event.merge_group.base_sha }}
        run: |
          if [[ "$GITHUB_EVENT_NAME" == "pull_request" ]]; then
            base="$(git merge-base origin/main HEAD)"
          elif [[ "$GITHUB_EVENT_NAME" == "merge_group" ]]; then
            base="$MERGE_GROUP_BASE_SHA"
            [ -n "$base" ] || { echo "merge_group event carried no base_sha"; exit 1; }
          else
            base="HEAD~1"
          fi
          python3 scripts/check_version_bumps.py --base "$base"

      - name: Explain skipped versioned surface bump gate
        if: github.event_name == 'workflow_dispatch'
        run: bash scripts/ci/report-manual-dispatch-skips.sh

      # A service-backed suite that skips itself reports green having compared
      # nothing. This pins both mechanisms that stop it: the LASH_REQUIRE_*
      # flags that turn a missing service into a failure, and the
      # --run-ignored/--include-ignored opt-in that the cross-backend
      # differential's `#[ignore]`d tests need in order to run at all.
      - name: Pin service gates
        run: python3 scripts/check_service_gate_pinning.py

      # GitHub rejects some workflow files that plain YAML parsing accepts
      # (e.g. expressions in contexts where they are not allowed); actionlint
      # validates against the real workflow schema. The release binary is
      # pinned by version and by the sha256 from the upstream release
      # checksums asset.
      - name: Lint GitHub workflows
        run: |
          curl -fsSL --retry 3 -o "${RUNNER_TEMP}/actionlint.tar.gz" \
            "https://github.com/rhysd/actionlint/releases/download/v1.7.10/actionlint_1.7.10_linux_amd64.tar.gz"
          echo "f4c76b71db5755a713e6055cbb0857ed07e103e028bda117817660ebadb4386f  ${RUNNER_TEMP}/actionlint.tar.gz" | sha256sum -c
          tar -xzf "${RUNNER_TEMP}/actionlint.tar.gz" -C "${RUNNER_TEMP}" actionlint
          "${RUNNER_TEMP}/actionlint" -no-color

      - name: Install Rust toolchain
        uses: ./.github/actions/rust-toolchain
        with:
          components: clippy, rustfmt


      - name: Restore cargo cache
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          cache-bin: false
          # The test-doc job is the sole writer of the shared linux-debug cache;
          # the lint job only reads it so clippy reuses the checked dependency
          # graph.
          shared-key: linux-debug
          save-if: false

      - name: Install protoc
        uses: ./.github/actions/install-tools
        with:
          tool: protoc

      - name: Install mold
        uses: ./.github/actions/setup-mold

      # `cargo fmt --all --check` and the same `--all-targets` clippy the local
      # push gate runs, plus the two boundary guards that otherwise gate nothing.
      # The clippy feature set mirrors the test-doc workspace check so the
      # dependency graph resolves identically.
      - name: Check formatting
        run: |
          cargo fmt --all --check
          # include!d files escape cargo fmt's module walk. The scoped scan
          # finds every one under the scopes it covers, so a new include! is
          # checked the moment it exists rather than when someone remembers to
          # add it here.
          python3 scripts/check_included_file_formatting.py

      - name: Configure Bazel shared cache
        if: needs.plan.outputs.bazel_trusted == 'true'
        uses: ./.github/actions/bazel-shared-cache
        env:
          CACHE_ENDPOINT: ${{ secrets.CACHE_ENDPOINT }}
          CACHE_INSTANCE: ${{ secrets.CACHE_INSTANCE }}
          KILN_EXECUTOR_RUNTIME: ${{ secrets.KILN_EXECUTOR_RUNTIME }}
          CACHE_CA: ${{ secrets.CACHE_CA }}
          CACHE_CERT: ${{ secrets.CACHE_CERT }}
          CACHE_KEY: ${{ secrets.CACHE_KEY }}

      # `//:workspace_clippy` runs one clippy action per first-party target of
      # the resolved default workspace graph — the `--workspace --all-targets`
      # shape — with the `[workspace.lints]` table, the nearest `clippy.toml`
      # and a trailing `-D warnings`, in the order Cargo emits them. See
      # `tools/bazel/clippy.bzl`.
      - name: Clippy (workspace, all targets, shared cache)
        if: needs.plan.outputs.bazel_trusted == 'true'
        shell: bash
        run: |
          # shellcheck disable=SC2086
          bazel --output_user_root="$RUNNER_TEMP/bazel-output" build \
            ${BAZEL_SHARED_CACHE_FLAGS} \
            --remote_download_outputs=minimal \
            //:workspace_clippy

      - name: Remove client credentials
        if: always()
        shell: bash
        run: rm -rf -- "$RUNNER_TEMP/build-cache"

      # Untrusted pull requests receive no cache credentials and keep exactly
      # the Cargo command that predates the Bazel branch above.
      - name: Clippy (workspace, all targets)
        if: needs.plan.outputs.bazel_trusted != 'true'
        run: |
          # shellcheck disable=SC2086
          cargo clippy --workspace --all-targets --locked ${LASH_CI_FEATURES} -- -D warnings

      # `slack-clone`'s `e2e` feature is outside the resolved default workspace
      # graph, so its `cfg(feature = "e2e")` code has no Bazel label. This
      # command stays Cargo on every event.
      - name: Clippy (slack-clone e2e feature)
        run: cargo clippy -p slack-clone --all-targets --features e2e --locked --no-deps -- -D warnings

      - name: Check Restate handler panic boundary
        run: python3 scripts/check-restate-handler-panics.py

      - name: Enforce durable transcript justification
        env:
          # Needed only by the dispatch path, where the justification has to be
          # read from the pull request rather than the event payload.
          GITHUB_TOKEN: ${{ github.token }}
        run: python3 scripts/check-transcript-diff.py --enforce

      - name: Check PostgreSQL payload-shape component version
        run: |
          python3 scripts/check-postgres-json-carrier-coverage.py
          python3 scripts/check-postgres-payload-shape-version.py

      # The recreation E2E's fixtures are pinned to the newest component
      # generation. Catch a stale pin here rather than one container gate later.
      - name: Check version-bump recreation fixtures
        run: python3 scripts/check_version_bump_fixtures.py

      - name: Check core/UI boundary
        run: bash scripts/check-core-ui-boundary.sh

      - name: Check substrate boundary
        run: bash scripts/check-substrate-boundary.sh

      - name: Check workflow graph model
        run: bash scripts/check-workflow-graph-model.sh

      - name: Check production file-size budget
        run: bash scripts/check-production-file-size.sh

  unicode-tests:
    name: Test deferred Unicode suites
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    needs: plan
    if: >-
      needs.plan.outputs.regress == 'true'
      || github.event_name == 'push'
      || github.event_name == 'workflow_dispatch'
    environment: build-cache
    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch --no-tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force FETCH_HEAD

      - name: Install Rust toolchain
        uses: ./.github/actions/rust-toolchain

      - name: Configure shared build cache
        if: needs.plan.outputs.bazel_trusted == 'true'
        uses: ./.github/actions/bazel-shared-cache
        env:
          CACHE_ENDPOINT: ${{ secrets.CACHE_ENDPOINT }}
          CACHE_INSTANCE: ${{ secrets.CACHE_INSTANCE }}
          KILN_EXECUTOR_RUNTIME: ${{ secrets.KILN_EXECUTOR_RUNTIME }}
          CACHE_CA: ${{ secrets.CACHE_CA }}
          CACHE_CERT: ${{ secrets.CACHE_CERT }}
          CACHE_KEY: ${{ secrets.CACHE_KEY }}

      - name: Test deferred Unicode suites (Bazel)
        if: needs.plan.outputs.bazel_trusted == 'true'
        shell: bash
        run: |
          # shellcheck disable=SC2086
          bazel --output_user_root="$BAZEL_OUTPUT_USER_ROOT" test \
            ${BAZEL_SHARED_CACHE_FLAGS} \
            --cache_test_results=yes --test_output=errors \
            //:deferred_tests

      - name: Test deferred Unicode suites (Cargo)
        if: needs.plan.outputs.bazel_trusted != 'true'
        run: cargo test -p lash-regress --test unicodesets --locked

      - name: Remove client credentials
        if: always() && needs.plan.outputs.bazel_trusted == 'true'
        shell: bash
        run: rm -rf -- "$RUNNER_TEMP/build-cache"

  postgres-store:
    name: Test Postgres store (PG ${{ matrix.postgres }}, ${{ matrix.role }})
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    needs: plan
    # Trusted events build every binary below from the shared cache; the
    # PostgreSQL version and connection settings are execution-only inputs, so
    # all three matrix legs reuse one set of compiled outputs. Untrusted events
    # receive no credentials and keep the Cargo compile. See
    # scripts/ci/store-tests.sh.
    environment: build-cache
    env:
      BAZEL_TRUSTED: ${{ needs.plan.outputs.bazel_trusted }}
      LASH_POSTGRES_DATABASE_URL: postgres://lash:lash@localhost:5432/lash
      LASH_REQUIRE_POSTGRES: "1"
    # The stable matrix always supplies real PostgreSQL evidence on pull requests
    # and merge groups. The heavier PG16 store/perf coverage remains deferred to
    # relevant trunk runs per the 2026-08-25 CI-scope ruling.
    if: github.event_name != 'push' && needs.plan.outputs.stores == 'true'

    # The structural schema check at open (`SchemaProvisioning`/`SchemaCheck`)
    # compares the live catalog against a generated expectation artifact, so a
    # catalog attribute that renders differently on a major lash has not tested
    # would reject every host on that major with no remedy but a lash release.
    # 14 and 18 bracket the supported range — 18 is where NOT NULL became
    # `pg_constraint` rows, 14 is before `pg_index.indnullsnotdistinct` existed —
    # and the shape suite asserts all three produce the byte-identical artifact.
    # PG14 and PG18 are compatibility lanes: they run only the live catalog
    # artifact comparison and a focused version-stamp gate, so they are deferred
    # off the pull-request critical path and run on merge_group, push and
    # workflow_dispatch. Nothing reaches trunk without all three majors: the
    # merge queue runs the full matrix before anything lands, and so does every
    # push to main. scripts/ci_plan.py `postgres-matrix` owns the per-event
    # matrix. PG16 is the sole primary lane and runs on every event. On PR-class events it runs the proven runtime identity oracle;
    # on trunk/full runs it retains the package-wide, pool-wait, runtime, and
    # cross-backend behavior coverage without triplicating it across majors.
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.plan.outputs.postgres_matrix) }}

    services:
      postgres:
        image: postgres:${{ matrix.postgres }}-alpine
        command: -c shared_preload_libraries=pg_stat_statements
        env:
          POSTGRES_USER: lash
          POSTGRES_PASSWORD: lash
          POSTGRES_DB: lash
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U lash -d lash"
          --health-interval 2s
          --health-timeout 2s
          --health-retries 30

    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --no-tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force FETCH_HEAD

      - name: Configure shared build cache
        if: needs.plan.outputs.bazel_trusted == 'true'
        uses: ./.github/actions/bazel-shared-cache
        env:
          CACHE_ENDPOINT: ${{ secrets.CACHE_ENDPOINT }}
          CACHE_INSTANCE: ${{ secrets.CACHE_INSTANCE }}
          KILN_EXECUTOR_RUNTIME: ${{ secrets.KILN_EXECUTOR_RUNTIME }}
          CACHE_CA: ${{ secrets.CACHE_CA }}
          CACHE_CERT: ${{ secrets.CACHE_CERT }}
          CACHE_KEY: ${{ secrets.CACHE_KEY }}

      - name: Install Rust toolchain
        if: needs.plan.outputs.bazel_trusted != 'true'
        uses: ./.github/actions/rust-toolchain

      - name: Restore cargo cache
        if: needs.plan.outputs.bazel_trusted != 'true'
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          cache-bin: false
          shared-key: linux-debug
          save-if: false

      - name: Install protoc and nextest
        if: needs.plan.outputs.bazel_trusted != 'true'
        uses: ./.github/actions/install-tools
        with:
          tool: protoc,nextest

      # Provides the linker the workflow-level RUSTFLAGS names. This job
      # restores the `linux-debug` cache and installs its linker directly.
      - name: Install mold
        if: needs.plan.outputs.bazel_trusted != 'true'
        uses: ./.github/actions/setup-mold

      # Required mode makes a missing URL fail, so missing CI configuration
      # cannot silently skip a lane.
      - name: Test PostgreSQL catalog compatibility
        if: matrix.role == 'compatibility'
        run: bash scripts/ci/store-tests.sh pg-catalog-compatibility

      # `lash-postgres-store`'s conformance tests run the backend-agnostic
      # RuntimePersistence / SessionStoreFactory / ProcessRegistry /
      # TriggerStore / LashlangArtifactStore suites against a real Postgres.
      # Required mode fails when the URL is unset, so missing CI configuration
      # cannot silently skip this lane. `connect()` applies the crate's
      # `schema.sql` artifact itself, so no separate migrate step. The same run
      # carries the `schema_shape` and `schema_drift` suites, which assert this
      # major produces the committed expectation artifact byte-for-byte and that
      # each seeded drift class is rejected by name.
      # The suites self-serialize on a shared in-process guard (they share one
      # database and reset() truncates all lash_* tables), so no --test-threads=1.
      # That guard is per-process, so this must stay `cargo test` — nextest's
      # process-per-test model would race the suites against each other.
      # Keep this package-wide: tests/attempt_atomicity.rs is part of this gate;
      # narrowing to --test conformance would silently drop attempt atomicity.
      - name: Test Postgres store (conformance and attempt atomicity)
        if: matrix.role == 'primary' && github.event_name != 'pull_request' && github.event_name != 'merge_group'
        run: bash scripts/ci/store-tests.sh pg-store

      - name: Test runtime pool-wait binding
        if: matrix.role == 'primary' && github.event_name != 'pull_request' && github.event_name != 'merge_group'
        run: bash scripts/ci/store-tests.sh pg-pool-wait

      # crates/lash currently has one Agent Scenario gated by the shared
      # LASH_POSTGRES_DATABASE_URL/LASH_REQUIRE_POSTGRES helper. It is also the
      # test that caught a real identity-version mutation, so keep it stable and
      # focused instead of scheduling tests by parsing changed source literals.
      - name: Test runtime Postgres agent scenarios
        if: matrix.role == 'primary'
        run: bash scripts/ci/store-tests.sh pg-agent-scenario

      # The full differential compares backend semantics, not catalog rendering,
      # so it runs once on the primary major instead of three times.
      - name: Test cross-backend store differential
        if: matrix.role == 'primary' && github.event_name != 'pull_request' && github.event_name != 'merge_group'
        run: bash scripts/ci/store-tests.sh pg-cross-backend
        env:
          LASH_CROSS_BACKEND_CASES: "4"

      - name: Remove client credentials
        if: always() && needs.plan.outputs.bazel_trusted == 'true'
        shell: bash
        run: rm -rf -- "$RUNNER_TEMP/build-cache"

  s3-store:
    name: Test S3 store against MinIO
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    needs: plan
    # Deferred to trunk runs (push/dispatch) per the 2026-08-25 CI-scope
    # ruling; reassess after the FIG-2169 test-prune sweep.
    if: github.event_name != 'pull_request' && github.event_name != 'merge_group' && needs.plan.outputs.stores == 'true'
    # Trusted events build these binaries from the shared cache and execute
    # them uncached against the MinIO this job starts. See
    # scripts/ci/store-tests.sh.
    environment: build-cache
    env:
      BAZEL_TRUSTED: ${{ needs.plan.outputs.bazel_trusted }}
      LASH_REQUIRE_MINIO: "1"

    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --no-tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force FETCH_HEAD

      - name: Configure shared build cache
        if: needs.plan.outputs.bazel_trusted == 'true'
        uses: ./.github/actions/bazel-shared-cache
        env:
          CACHE_ENDPOINT: ${{ secrets.CACHE_ENDPOINT }}
          CACHE_INSTANCE: ${{ secrets.CACHE_INSTANCE }}
          KILN_EXECUTOR_RUNTIME: ${{ secrets.KILN_EXECUTOR_RUNTIME }}
          CACHE_CA: ${{ secrets.CACHE_CA }}
          CACHE_CERT: ${{ secrets.CACHE_CERT }}
          CACHE_KEY: ${{ secrets.CACHE_KEY }}

      - name: Install Rust toolchain
        if: needs.plan.outputs.bazel_trusted != 'true'
        uses: ./.github/actions/rust-toolchain

      - name: Restore cargo cache
        if: needs.plan.outputs.bazel_trusted != 'true'
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          cache-bin: false
          shared-key: linux-debug
          save-if: false

      - name: Install protoc
        if: needs.plan.outputs.bazel_trusted != 'true'
        uses: ./.github/actions/install-tools
        with:
          tool: protoc

      # Provides the linker the workflow-level RUSTFLAGS names. This job
      # restores the `linux-debug` cache and installs its linker directly.
      - name: Install mold
        if: needs.plan.outputs.bazel_trusted != 'true'
        uses: ./.github/actions/setup-mold

      - name: Start MinIO and create test bucket
        run: |
          # Pull both images up front with retries so a transient Docker Hub
          # error fails the pull instead of leaving `docker run` to report
          # "No such image" and take the whole trunk run red.
          bash scripts/docker-pull-with-retry.sh quay.io/minio/minio:RELEASE.2025-04-22T22-12-26Z
          bash scripts/docker-pull-with-retry.sh quay.io/minio/mc:RELEASE.2025-04-16T18-13-26Z
          docker run -d --name lash-ci-minio \
            -e MINIO_ROOT_USER=minioadmin \
            -e MINIO_ROOT_PASSWORD=minioadmin \
            -p 9000:9000 \
            quay.io/minio/minio:RELEASE.2025-04-22T22-12-26Z server /data
          for attempt in $(seq 1 60); do
            if docker run --rm --network host quay.io/minio/mc:RELEASE.2025-04-16T18-13-26Z \
              alias set ci http://127.0.0.1:9000 minioadmin minioadmin; then
              break
            fi
            if [ "$attempt" -eq 60 ]; then
              docker logs lash-ci-minio
              exit 1
            fi
            sleep 1
          done
          docker run --rm --network host --entrypoint /bin/sh quay.io/minio/mc:RELEASE.2025-04-16T18-13-26Z -c \
            'mc alias set ci http://127.0.0.1:9000 minioadmin minioadmin >/dev/null && mc mb --ignore-existing ci/lash-attachments'

      - name: Test S3 store conformance
        run: bash scripts/ci/store-tests.sh s3-store

      - name: Test attachment blob-store differential
        run: bash scripts/ci/store-tests.sh s3-attachment-differential

      - name: Stop MinIO
        if: always()
        run: docker rm -f lash-ci-minio

      - name: Remove client credentials
        if: always() && needs.plan.outputs.bazel_trusted == 'true'
        shell: bash
        run: rm -rf -- "$RUNNER_TEMP/build-cache"

  fuzz-smoke:
    name: Fuzz smoke
    runs-on: ubuntu-24.04
    # Explicit budget: four bounded libFuzzer runs over the committed seed
    # corpus plus one nightly build must never grow past this.
    timeout-minutes: 45
    needs: plan
    # Off the pull-request critical path by design (FIG-878): the smoke run
    # guards trunk pushes and dispatches only. scripts/test_ci_plan.py pins
    # this shape.
    if: github.event_name != 'pull_request' && github.event_name != 'merge_group' && needs.plan.outputs.rust == 'true'

    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git config gc.auto 0
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --no-tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force FETCH_HEAD

      - name: Check seed corpus integrity
        run: python3 scripts/check_fuzz_corpus.py

      - name: Install mold
        uses: ./.github/actions/setup-mold

      - name: Install nightly Rust and cargo-fuzz
        run: |
          set -euo pipefail
          rustup toolchain install nightly --profile minimal
          cargo install cargo-fuzz --locked

      - name: Install protoc
        uses: ./.github/actions/install-tools
        with:
          tool: protoc

      - name: Run bounded fuzz smoke over the committed corpus
        working-directory: fuzz
        run: |
          set -euo pipefail
          for target in remote_wire_dto plugin_payload attachment_metadata lashlang_source; do
            cargo +nightly fuzz run "$target" "corpus/$target" -- \
              -max_total_time=60 -timeout=10 -rss_limit_mb=4096
          done

  stack-budget:
    name: Stack budget
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    needs: plan
    # Deferred to trunk runs (push/dispatch) per the 2026-08-25 CI-scope
    # ruling; reassess after the FIG-2169 test-prune sweep.
    if: github.event_name != 'pull_request' && github.event_name != 'merge_group' && needs.plan.outputs.rust == 'true'

    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --no-tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force FETCH_HEAD

      - name: Install Rust toolchain
        uses: ./.github/actions/rust-toolchain

      - name: Restore cargo cache
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          cache-bin: false
          shared-key: linux-debug
          save-if: false

      - name: Install protoc
        uses: ./.github/actions/install-tools
        with:
          tool: protoc

      # Provides the linker the workflow-level RUSTFLAGS names. This job
      # restores the `linux-debug` cache and installs its linker directly.
      - name: Install mold
        uses: ./.github/actions/setup-mold

      - name: Enforce stack budget
        run: bash scripts/ci-stack-budget.sh

  worker-artifacts:
    name: Build worker release artifacts
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    needs: plan
    outputs:
      artifact_id: ${{ steps.upload-worker-binaries.outputs.artifact-id }}
      artifact_name: ${{ steps.prepare-worker-binaries.outputs.artifact_name }}
      producer_attempt: ${{ steps.prepare-worker-binaries.outputs.producer_attempt }}
    if: >-
      github.event_name == 'workflow_dispatch'
      || (github.event_name == 'push' && github.ref == 'refs/heads/main')
      || (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'ci:workers'))
    env:
      RUN_WORKER_ARTIFACTS: ${{ needs.plan.outputs.workers_e2e == 'true' || needs.plan.outputs.functional_e2e == 'true' || (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'ci:workers')) }}
    steps:
      - name: Explain plan deferral
        if: env.RUN_WORKER_ARTIFACTS != 'true'
        run: echo "Worker artifacts are not required by the CI plan."

      - name: Check out repository
        if: env.RUN_WORKER_ARTIFACTS == 'true'
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git config gc.auto 0
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --no-tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force "${GITHUB_SHA}"

      - name: Reclaim runner disk
        if: env.RUN_WORKER_ARTIFACTS == 'true'
        run: bash scripts/ci-reclaim-disk.sh

      - name: Install Rust toolchain
        if: env.RUN_WORKER_ARTIFACTS == 'true'
        uses: ./.github/actions/rust-toolchain

      - name: Install protoc
        if: env.RUN_WORKER_ARTIFACTS == 'true'
        uses: ./.github/actions/install-tools
        with:
          tool: protoc

      - name: Install mold
        if: env.RUN_WORKER_ARTIFACTS == 'true'
        uses: ./.github/actions/setup-mold

      - name: Restore worker release dependencies
        if: env.RUN_WORKER_ARTIFACTS == 'true'
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          cache-bin: false
          shared-key: linux-worker-release
          cache-workspace-crates: true
          save-if: true

      - name: Build worker binaries once
        if: env.RUN_WORKER_ARTIFACTS == 'true'
        run: |
          cargo build --locked --release -p lash-restate-postgres-workers-e2e --bins
          mkdir -p "${RUNNER_TEMP}/worker-artifacts"
          find target/release -maxdepth 1 -type f -name 'lash-e2e-*' -executable \
            -exec cp '{}' "${RUNNER_TEMP}/worker-artifacts/" \;
          tar -C "${RUNNER_TEMP}/worker-artifacts" -cf "${RUNNER_TEMP}/worker-binaries.tar" .

      - name: Record worker binary provenance
        id: prepare-worker-binaries
        if: env.RUN_WORKER_ARTIFACTS == 'true'
        env:
          ARTIFACT_NAME: worker-binaries-${{ github.sha }}-${{ github.run_attempt }}
        run: |
          python3 scripts/ci/artifact_provenance.py create \
            --manifest "${RUNNER_TEMP}/worker-binaries.provenance.json" \
            --payload-root "${RUNNER_TEMP}" \
            --payload worker-binaries.tar \
            --artifact-name "${ARTIFACT_NAME}" \
            --producer-job worker-artifacts \
            --producer-instance worker-binaries \
            --producer-attempt "${GITHUB_RUN_ATTEMPT}"
          echo "artifact_name=${ARTIFACT_NAME}" >> "${GITHUB_OUTPUT}"
          echo "producer_attempt=${GITHUB_RUN_ATTEMPT}" >> "${GITHUB_OUTPUT}"

      - name: Upload worker binaries
        id: upload-worker-binaries
        if: env.RUN_WORKER_ARTIFACTS == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ steps.prepare-worker-binaries.outputs.artifact_name }}
          path: |
            ${{ runner.temp }}/worker-binaries.tar
            ${{ runner.temp }}/worker-binaries.provenance.json
          if-no-files-found: error
          compression-level: 0
          retention-days: 1

  functional-e2e:
    name: Functional E2E (${{ matrix.name }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 40
    needs: plan
    # Deferred to trunk runs (push/dispatch) per the 2026-08-25 CI-scope
    # ruling; reassess after the FIG-2169 test-prune sweep.
    if: github.event_name != 'pull_request' && github.event_name != 'merge_group' && needs.plan.outputs.functional_e2e == 'true'

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: agent-service
            recipe: agent-service-restate-e2e
            node: false
            artifact: none
            browser: false
            runner: ubuntu-24.04
          - name: agent-workbench
            recipe: agent-workbench-restate-e2e
            node: false
            artifact: none
            browser: false
            runner: ubuntu-24.04
          - name: effect-group-conformance
            recipe: effect-group-conformance-e2e
            node: false
            artifact: none
            browser: false
            runner: ubuntu-24.04
          - name: workflow-graph-roundtrip
            recipe: workflow-graph-integration-verify
            node: true
            artifact: none
            browser: false
            runner: ubuntu-24.04
          - name: version-bump-recreation
            recipe: version-bump-recreation-e2e
            node: false
            artifact: version-bump-recreation
            browser: false
            runner: ubuntu-24.04
          - name: session-lease-triage
            recipe: session-lease-triage-e2e
            node: false
            artifact: session-lease-triage
            browser: false
            runner: ubuntu-24.04
          - name: slack-clone-full-host
            recipe: slack-clone-full-host-e2e
            node: false
            artifact: none
            browser: true
            runner: ubuntu-24.04

    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --no-tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force FETCH_HEAD

      - name: Reclaim runner disk
        run: bash scripts/ci-reclaim-disk.sh

      - name: Install Rust toolchain
        uses: ./.github/actions/rust-toolchain


      - name: Restore cargo cache
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          cache-bin: false
          shared-key: linux-debug
          save-if: false

      - name: Install protoc
        uses: ./.github/actions/install-tools
        with:
          tool: protoc

      # Provides the linker the workflow-level RUSTFLAGS names. This job
      # restores the `linux-debug` cache and installs its linker directly.
      - name: Install mold
        uses: ./.github/actions/setup-mold

      - name: Install Node
        if: matrix.node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: examples/workflow-graph-roundtrip/frontend/package-lock.json

      - name: Install uv
        if: matrix.browser
        uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0

      - name: Install Chromium for Playwright
        if: matrix.browser
        run: uv run --with playwright==1.62.0 playwright install --with-deps chromium

      # The repo-local installer uses checksum-pinned release archives instead
      # of the GitHub releases API; merge-queue trains fan out enough jobs that
      # the API-listing path (extractions/setup-just) hit rate limits and
      # ejected whole trains.
      - name: Install just
        uses: ./.github/actions/install-tools
        with:
          tool: just

      - name: Run functional E2E
        env:
          LASH_PROCESS_OPERATIONS_ARTIFACT_DIR: target/functional-e2e-artifacts/process-operations
          LASH_VERSION_BUMP_ARTIFACT_DIR: target/functional-e2e-artifacts/version-bump-recreation
          LASH_SESSION_LEASE_ARTIFACT_DIR: target/functional-e2e-artifacts/session-lease-triage
          LASH_SLACK_CLONE_E2E_ARTIFACT_DIR: ${{ matrix.browser && format('{0}/slack-clone-full-host-e2e', runner.temp) || '' }}
        run: just ${{ matrix.recipe }}

      - name: Upload functional E2E failure artifacts
        if: failure() && matrix.artifact != 'none'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: functional-e2e-${{ matrix.artifact }}-attempt-${{ github.run_attempt }}
          path: target/functional-e2e-artifacts/${{ matrix.artifact }}
          if-no-files-found: warn

      - name: Upload slack-clone full-host E2E failure artifacts
        if: failure() && matrix.browser
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: slack-clone-full-host-e2e-attempt-${{ github.run_attempt }}
          path: ${{ runner.temp }}/slack-clone-full-host-e2e
          if-no-files-found: warn

  functional-e2e-process-operations:
    name: Functional E2E (${{ matrix.name }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 40
    needs: [plan, worker-artifacts]
    # Deferred to trunk runs (push/dispatch) per the 2026-08-25 CI-scope
    # ruling; reassess after the FIG-2169 test-prune sweep.
    if: github.event_name != 'pull_request' && github.event_name != 'merge_group' && needs.plan.outputs.functional_e2e == 'true'

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: process-operations
            recipe: process-operations-e2e
            node: false
            artifact: process-operations
            browser: false
            runner: ubuntu-24.04

    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git config gc.auto 0
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --no-tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force "${GITHUB_SHA}"

      - name: Reclaim runner disk
        run: bash scripts/ci-reclaim-disk.sh

      - name: Install Rust toolchain
        uses: ./.github/actions/rust-toolchain


      - name: Restore cargo cache
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          cache-bin: false
          shared-key: linux-debug
          save-if: false

      - name: Install protoc
        uses: ./.github/actions/install-tools
        with:
          tool: protoc

      # Provides the linker the workflow-level RUSTFLAGS names. This job
      # restores the `linux-debug` cache and installs its linker directly.
      - name: Install mold
        uses: ./.github/actions/setup-mold

      # The repo-local installer uses checksum-pinned release archives instead
      # of the GitHub releases API; merge-queue trains fan out enough jobs that
      # the API-listing path (extractions/setup-just) hit rate limits and
      # ejected whole trains.
      - name: Install just
        uses: ./.github/actions/install-tools
        with:
          tool: just

      # Each recipe owns its dependencies and any container lifecycle, so the
      # matrix legs run independently without cross-job coordination.
      - name: Require exact worker artifact selection
        run: >-
          python3 scripts/ci/artifact_provenance.py select
          --artifact-name "${{ needs.worker-artifacts.outputs.artifact_name }}"
          --expected-artifact-name "worker-binaries-${GITHUB_SHA}-${{ needs.worker-artifacts.outputs.producer_attempt }}"
          --selected-artifact-id "${{ needs.worker-artifacts.outputs.artifact_id }}"
          --producer-attempt "${{ needs.worker-artifacts.outputs.producer_attempt }}"
          --current-attempt "${GITHUB_RUN_ATTEMPT}"

      - name: Download worker binaries
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          artifact-ids: ${{ needs.worker-artifacts.outputs.artifact_id }}
          path: ${{ runner.temp }}/worker-download

      - name: Verify and extract worker binaries
        run: |
          python3 scripts/ci/artifact_provenance.py consume \
            --manifest "${RUNNER_TEMP}/worker-download/worker-binaries.provenance.json" \
            --payload-root "${RUNNER_TEMP}/worker-download" \
            --require-payload worker-binaries.tar \
            --artifact-name "${{ needs.worker-artifacts.outputs.artifact_name }}" \
            --producer-job worker-artifacts \
            --producer-instance worker-binaries \
            --producer-attempt "${{ needs.worker-artifacts.outputs.producer_attempt }}" \
            --selected-artifact-id "${{ needs.worker-artifacts.outputs.artifact_id }}" \
            --current-attempt "${GITHUB_RUN_ATTEMPT}" \
            --receipt "${RUNNER_TEMP}/worker-binaries-consumption.json" \
            --step-summary "${GITHUB_STEP_SUMMARY}" \
            --extract-tar worker-binaries.tar \
            --extract-to "${RUNNER_TEMP}/worker-binaries"
          echo "LASH_E2E_PREBUILT_BIN_DIR=${RUNNER_TEMP}/worker-binaries" >> "$GITHUB_ENV"

      - name: Run functional E2E
        env:
          LASH_PROCESS_OPERATIONS_ARTIFACT_DIR: target/functional-e2e-artifacts/process-operations
          LASH_VERSION_BUMP_ARTIFACT_DIR: target/functional-e2e-artifacts/version-bump-recreation
          LASH_SESSION_LEASE_ARTIFACT_DIR: target/functional-e2e-artifacts/session-lease-triage
          LASH_SLACK_CLONE_E2E_ARTIFACT_DIR: ${{ matrix.browser && format('{0}/slack-clone-full-host-e2e', runner.temp) || '' }}
        run: just ${{ matrix.recipe }}

      - name: Upload functional E2E failure artifacts
        if: failure() && matrix.artifact != 'none'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: functional-e2e-${{ matrix.artifact }}-attempt-${{ github.run_attempt }}
          path: target/functional-e2e-artifacts/${{ matrix.artifact }}
          if-no-files-found: warn

  restate-postgres-workers:
    name: Restate + Postgres + MinIO Workers (segment ${{ matrix.segment }})
    runs-on: ubuntu-24.04
    # This label-aware guard replaces `if: github.event_name != 'push' || github.ref == 'refs/heads/main'`.
    # Worker steps formerly repeated `if: (github.event_name != 'push' || github.ref == 'refs/heads/main') && needs.plan.outputs.workers_e2e == 'true'`.
    if: >-
      github.event_name == 'workflow_dispatch'
      || (github.event_name == 'push' && github.ref == 'refs/heads/main')
      || (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'ci:workers'))
    # The harness has grown to 24 workflows + turn-control gates; 30 minutes
    # was hit on 2026-07-20 (job-timeout shows as 'cancelled').
    timeout-minutes: 75
    needs: [plan, worker-artifacts]
    outputs:
      segment_1_artifact_id: ${{ steps.upload-segment-1.outputs.artifact-id }}
      segment_1_artifact_name: ${{ steps.prepare-segment-1.outputs.artifact_name }}
      segment_1_producer_attempt: ${{ steps.prepare-segment-1.outputs.producer_attempt }}
      segment_2_artifact_id: ${{ steps.upload-segment-2.outputs.artifact-id }}
      segment_2_artifact_name: ${{ steps.prepare-segment-2.outputs.artifact_name }}
      segment_2_producer_attempt: ${{ steps.prepare-segment-2.outputs.producer_attempt }}
    env:
      RUN_WORKERS_E2E: ${{ needs.plan.outputs.workers_e2e == 'true' || (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'ci:workers')) }}
    strategy:
      fail-fast: false
      matrix:
        segment: [1, 2]

    steps:
      - name: Explain dispatch plan deferral
        if: github.event_name == 'workflow_dispatch' && env.RUN_WORKERS_E2E != 'true'
        run: echo "Workers E2E execution is not required by the full-profile CI plan."

      - name: Explain plan deferral
        if: github.event_name != 'workflow_dispatch' && env.RUN_WORKERS_E2E != 'true'
        run: echo "Workers E2E execution is not required by the CI plan."

      - name: Check out repository
        if: env.RUN_WORKERS_E2E == 'true'
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --no-tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force FETCH_HEAD

      - name: Reclaim runner disk
        if: env.RUN_WORKERS_E2E == 'true'
        run: bash scripts/ci-reclaim-disk.sh

      - name: Install Rust toolchain
        if: env.RUN_WORKERS_E2E == 'true'
        uses: ./.github/actions/rust-toolchain

      # Release binaries come from worker-artifacts. Segment 1 also builds the
      # S3 tests in debug; retain those workspace outputs in a dedicated cache.
      - name: Restore cargo cache
        if: env.RUN_WORKERS_E2E == 'true'
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          cache-bin: false
          shared-key: linux-worker-tests
          cache-workspace-crates: true
          save-if: ${{ matrix.segment == 1 }}

      - name: Install protoc
        if: env.RUN_WORKERS_E2E == 'true'
        uses: ./.github/actions/install-tools
        with:
          tool: protoc

      # Provides the linker the workflow-level RUSTFLAGS names. This job
      # restores the `linux-worker-tests` cache and installs its linker directly.
      - name: Install mold
        if: env.RUN_WORKERS_E2E == 'true'
        uses: ./.github/actions/setup-mold

      - name: Require exact worker artifact selection
        if: env.RUN_WORKERS_E2E == 'true'
        run: >-
          python3 scripts/ci/artifact_provenance.py select
          --artifact-name "${{ needs.worker-artifacts.outputs.artifact_name }}"
          --expected-artifact-name "worker-binaries-${GITHUB_SHA}-${{ needs.worker-artifacts.outputs.producer_attempt }}"
          --selected-artifact-id "${{ needs.worker-artifacts.outputs.artifact_id }}"
          --producer-attempt "${{ needs.worker-artifacts.outputs.producer_attempt }}"
          --current-attempt "${GITHUB_RUN_ATTEMPT}"

      - name: Download worker binaries
        if: env.RUN_WORKERS_E2E == 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          artifact-ids: ${{ needs.worker-artifacts.outputs.artifact_id }}
          path: ${{ runner.temp }}/worker-download

      - name: Verify and extract worker binaries
        if: env.RUN_WORKERS_E2E == 'true'
        run: |
          python3 scripts/ci/artifact_provenance.py consume \
            --manifest "${RUNNER_TEMP}/worker-download/worker-binaries.provenance.json" \
            --payload-root "${RUNNER_TEMP}/worker-download" \
            --require-payload worker-binaries.tar \
            --artifact-name "${{ needs.worker-artifacts.outputs.artifact_name }}" \
            --producer-job worker-artifacts \
            --producer-instance worker-binaries \
            --producer-attempt "${{ needs.worker-artifacts.outputs.producer_attempt }}" \
            --selected-artifact-id "${{ needs.worker-artifacts.outputs.artifact_id }}" \
            --current-attempt "${GITHUB_RUN_ATTEMPT}" \
            --receipt "target/restate-postgres-workers-e2e-manifests/segment-${{ matrix.segment }}/worker-binaries-consumption.json" \
            --step-summary "${GITHUB_STEP_SUMMARY}" \
            --extract-tar worker-binaries.tar \
            --extract-to "${RUNNER_TEMP}/worker-binaries"
          echo "LASH_E2E_PREBUILT_BIN_DIR=${RUNNER_TEMP}/worker-binaries" >> "$GITHUB_ENV"

      - name: Run distributed backend E2E
        if: env.RUN_WORKERS_E2E == 'true'
        env:
          LASH_E2E_WORKFLOW_SEGMENT: ${{ matrix.segment }}
          LASH_E2E_COMPLETED_WORKFLOW_MANIFEST: target/restate-postgres-workers-e2e-manifests/segment-${{ matrix.segment }}/completed-workflows.txt
        run: bash scripts/restate-postgres-workers-e2e.sh

      - name: Run graceful drain operator-flow E2E
        if: env.RUN_WORKERS_E2E == 'true' && matrix.segment == 1
        env:
          LASH_GRACEFUL_DRAIN_ARTIFACT_DIR: target/operator-flow-artifacts/graceful-drain
        run: bash scripts/graceful-drain-e2e.sh

      - name: Run request abandon operator-flow E2E
        if: env.RUN_WORKERS_E2E == 'true' && matrix.segment == 1
        env:
          LASH_REQUEST_ABANDON_ARTIFACT_DIR: target/operator-flow-artifacts/request-abandon
        run: bash scripts/request-abandon-e2e.sh

      - name: Upload operator-flow E2E failure artifacts
        if: failure() && env.RUN_WORKERS_E2E == 'true' && matrix.segment == 1
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: operator-flow-e2e-attempt-${{ github.run_attempt }}
          path: target/operator-flow-artifacts
          if-no-files-found: warn

      - name: Record segment 1 result provenance
        id: prepare-segment-1
        if: always() && env.RUN_WORKERS_E2E == 'true' && matrix.segment == 1
        env:
          ARTIFACT_NAME: restate-workers-e2e-segment-1-attempt-${{ github.run_attempt }}
        run: |
          python3 scripts/ci/artifact_provenance.py create \
            --manifest target/restate-postgres-workers-e2e-manifests/segment-1/provenance.json \
            --payload-root target/restate-postgres-workers-e2e-manifests/segment-1 \
            --payload workflow-inventory.tsv \
            --payload completed-workflows.txt \
            --payload worker-binaries-consumption.json \
            --artifact-name "${ARTIFACT_NAME}" \
            --producer-job restate-postgres-workers \
            --producer-instance segment-1 \
            --producer-attempt "${GITHUB_RUN_ATTEMPT}"
          echo "artifact_name=${ARTIFACT_NAME}" >> "${GITHUB_OUTPUT}"
          echo "producer_attempt=${GITHUB_RUN_ATTEMPT}" >> "${GITHUB_OUTPUT}"

      - name: Upload segment 1 completed workflow manifest
        id: upload-segment-1
        if: always() && env.RUN_WORKERS_E2E == 'true' && matrix.segment == 1
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ steps.prepare-segment-1.outputs.artifact_name }}
          path: target/restate-postgres-workers-e2e-manifests/segment-1
          if-no-files-found: error

      - name: Record segment 2 result provenance
        id: prepare-segment-2
        if: always() && env.RUN_WORKERS_E2E == 'true' && matrix.segment == 2
        env:
          ARTIFACT_NAME: restate-workers-e2e-segment-2-attempt-${{ github.run_attempt }}
        run: |
          python3 scripts/ci/artifact_provenance.py create \
            --manifest target/restate-postgres-workers-e2e-manifests/segment-2/provenance.json \
            --payload-root target/restate-postgres-workers-e2e-manifests/segment-2 \
            --payload workflow-inventory.tsv \
            --payload completed-workflows.txt \
            --payload worker-binaries-consumption.json \
            --artifact-name "${ARTIFACT_NAME}" \
            --producer-job restate-postgres-workers \
            --producer-instance segment-2 \
            --producer-attempt "${GITHUB_RUN_ATTEMPT}"
          echo "artifact_name=${ARTIFACT_NAME}" >> "${GITHUB_OUTPUT}"
          echo "producer_attempt=${GITHUB_RUN_ATTEMPT}" >> "${GITHUB_OUTPUT}"

      - name: Upload segment 2 completed workflow manifest
        id: upload-segment-2
        if: always() && env.RUN_WORKERS_E2E == 'true' && matrix.segment == 2
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ steps.prepare-segment-2.outputs.artifact_name }}
          path: target/restate-postgres-workers-e2e-manifests/segment-2
          if-no-files-found: error

  restate-postgres-workers-summary:
    name: Restate workers E2E coverage summary
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    # This label-aware guard replaces `if: always() && (github.event_name != 'push' || github.ref == 'refs/heads/main')`.
    # Summary steps formerly repeated `if: (github.event_name != 'push' || github.ref == 'refs/heads/main') && needs.plan.outputs.workers_e2e == 'true'`.
    if: >-
      always()
      && (github.event_name == 'workflow_dispatch'
      || (github.event_name == 'push' && github.ref == 'refs/heads/main')
      || (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'ci:workers')))
    needs:
      - plan
      - restate-postgres-workers
    env:
      RUN_WORKERS_E2E: ${{ needs.plan.outputs.workers_e2e == 'true' || (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'ci:workers')) }}

    steps:
      - name: Check out repository
        if: env.RUN_WORKERS_E2E == 'true'
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --no-tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force FETCH_HEAD

      - name: Require exact segment result selections
        if: env.RUN_WORKERS_E2E == 'true'
        run: |
          python3 scripts/ci/artifact_provenance.py select \
            --artifact-name "${{ needs.restate-postgres-workers.outputs.segment_1_artifact_name }}" \
            --expected-artifact-name "restate-workers-e2e-segment-1-attempt-${{ needs.restate-postgres-workers.outputs.segment_1_producer_attempt }}" \
            --selected-artifact-id "${{ needs.restate-postgres-workers.outputs.segment_1_artifact_id }}" \
            --producer-attempt "${{ needs.restate-postgres-workers.outputs.segment_1_producer_attempt }}" \
            --current-attempt "${GITHUB_RUN_ATTEMPT}"
          python3 scripts/ci/artifact_provenance.py select \
            --artifact-name "${{ needs.restate-postgres-workers.outputs.segment_2_artifact_name }}" \
            --expected-artifact-name "restate-workers-e2e-segment-2-attempt-${{ needs.restate-postgres-workers.outputs.segment_2_producer_attempt }}" \
            --selected-artifact-id "${{ needs.restate-postgres-workers.outputs.segment_2_artifact_id }}" \
            --producer-attempt "${{ needs.restate-postgres-workers.outputs.segment_2_producer_attempt }}" \
            --current-attempt "${GITHUB_RUN_ATTEMPT}"

      - name: Download segment 1 completed workflow manifest
        if: env.RUN_WORKERS_E2E == 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          artifact-ids: ${{ needs.restate-postgres-workers.outputs.segment_1_artifact_id }}
          path: target/restate-postgres-workers-e2e-download/segment-1

      - name: Download segment 2 completed workflow manifest
        if: env.RUN_WORKERS_E2E == 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          artifact-ids: ${{ needs.restate-postgres-workers.outputs.segment_2_artifact_id }}
          path: target/restate-postgres-workers-e2e-download/segment-2

      - name: Verify completed workflow manifest provenance
        if: env.RUN_WORKERS_E2E == 'true'
        run: |
          for segment in 1 2; do
            artifact_name="${{ needs.restate-postgres-workers.outputs.segment_1_artifact_name }}"
            artifact_id="${{ needs.restate-postgres-workers.outputs.segment_1_artifact_id }}"
            producer_attempt="${{ needs.restate-postgres-workers.outputs.segment_1_producer_attempt }}"
            if [[ "${segment}" == 2 ]]; then
              artifact_name="${{ needs.restate-postgres-workers.outputs.segment_2_artifact_name }}"
              artifact_id="${{ needs.restate-postgres-workers.outputs.segment_2_artifact_id }}"
              producer_attempt="${{ needs.restate-postgres-workers.outputs.segment_2_producer_attempt }}"
            fi
            python3 scripts/ci/artifact_provenance.py consume \
              --manifest "target/restate-postgres-workers-e2e-download/segment-${segment}/provenance.json" \
              --payload-root "target/restate-postgres-workers-e2e-download/segment-${segment}" \
              --require-payload workflow-inventory.tsv \
              --require-payload completed-workflows.txt \
              --require-payload worker-binaries-consumption.json \
              --artifact-name "${artifact_name}" \
              --producer-job restate-postgres-workers \
              --producer-instance "segment-${segment}" \
              --producer-attempt "${producer_attempt}" \
              --selected-artifact-id "${artifact_id}" \
              --current-attempt "${GITHUB_RUN_ATTEMPT}" \
              --receipt "${RUNNER_TEMP}/worker-segment-${segment}-consumption.json" \
              --step-summary "${GITHUB_STEP_SUMMARY}"
          done

      - name: Verify complete workflow coverage
        if: env.RUN_WORKERS_E2E == 'true'
        run: >-
          python3 scripts/check_restate_workers_e2e_coverage.py
          --leg 1
          target/restate-postgres-workers-e2e-download/segment-1/workflow-inventory.tsv
          target/restate-postgres-workers-e2e-download/segment-1/completed-workflows.txt
          --leg 2
          target/restate-postgres-workers-e2e-download/segment-2/workflow-inventory.tsv
          target/restate-postgres-workers-e2e-download/segment-2/completed-workflows.txt

  diff-hygiene:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    env:
      BASE_REF: origin/main
      BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || '' }}
      # `github.sha` on a `pull_request` is the ephemeral `refs/pull/N/merge`
      # commit, whose second parent is main's tip. Deepening that commit walks
      # both parents, the two chains stop at different shallow boundaries, and
      # main commits the truncated graph cannot exclude get reported as if this
      # pull request had introduced them (PR #1226: 12 secret-scan findings,
      # every one fingerprinted to main commit 01677644b). Scan the pull
      # request's own head instead. The auto-generated merge commit resolves no
      # conflicts and so contributes no content of its own. On `merge_group`
      # this resolves to the queue's merge commit and on `push` to the pushed
      # commit, both equal to `github.sha`, so those events scan what they scan
      # today. The exclusion point is then the merge base, not `BASE_SHA`
      # itself: `pull_request.base.sha` is main's tip *now*, which is normally
      # not an ancestor of the head at all.
      SCAN_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.event.merge_group.head_sha || github.sha }}
      # Labels exist only on pull_request; the footer override also covers merge_group.
      DIFF_HYGIENE_BYPASS: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'diff-hygiene-override') && '1' || '0' }}
    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          # Background auto-gc rewrites .git/shallow between the deepen fetch and
          # the ancestry check ("shallow file has changed since we read it").
          git config gc.auto 0
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --no-tags --prune --depth=1 origin "${SCAN_HEAD_SHA}"
          git checkout --detach --force "${SCAN_HEAD_SHA}"
          # Deepen both sides in lockstep. A boundary on one chain that the
          # other has already walked past is what leaks foreign commits into
          # the range, and `--unshallow` on an already-complete repository is a
          # fatal error rather than a no-op.
          unshallow_if_needed() {
            if [[ "$(git rev-parse --is-shallow-repository)" == "true" ]]; then
              git fetch --no-tags --unshallow origin
            fi
          }
          main_refspec='+refs/heads/main:refs/remotes/origin/main'
          if [[ -n "${BASE_SHA}" ]]; then
            git fetch --no-tags --depth=1 origin "${BASE_SHA}"
            depth=50
            until git merge-base "${BASE_SHA}" HEAD >/dev/null 2>&1; do
              if [[ ${depth} -gt 1600 ]]; then
                unshallow_if_needed
                break
              fi
              git fetch --no-tags --deepen="${depth}" origin "${SCAN_HEAD_SHA}" "${BASE_SHA}"
              depth=$((depth * 2))
            done
            git merge-base "${BASE_SHA}" HEAD >/dev/null
          else
            git fetch --no-tags --depth=1 origin "${main_refspec}"
            depth=50
            until git merge-base origin/main HEAD >/dev/null 2>&1; do
              if [[ ${depth} -gt 1600 ]]; then
                unshallow_if_needed
                break
              fi
              git fetch --no-tags --deepen="${depth}" origin "${SCAN_HEAD_SHA}" "${main_refspec}"
              depth=$((depth * 2))
            done
            git merge-base origin/main HEAD >/dev/null
          fi
          # A merge base existing is not the same as the whole shared ancestry
          # being connected. `--deepen` extends every shallow boundary, so a
          # branch that merged main in earlier reaches main through a second
          # chain rooted much further down; the loop stops at the first merge
          # base, and the main commits that chain exposes are never covered by
          # the base's own boundary, so the range does not exclude them. PR
          # #1226 head c5aad0ae6 carries ten such merges and reported 12
          # findings, all fingerprinted to 0625be3de, an ancestor of main.
          # With no merge in the range there is a single chain and the shallow
          # graph is already exact; with merges, only a complete graph excludes
          # correctly.
          if [[ -n "${BASE_SHA}" ]]; then
            range_base="$(git merge-base "${BASE_SHA}" HEAD)"
          else
            range_base="$(git merge-base origin/main HEAD)"
          fi
          if [[ "$(git rev-list --merges --count "${range_base}..HEAD")" -gt 0 ]]; then
            unshallow_if_needed
          fi
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0

      - name: Check diff hygiene
        run: bash scripts/ci/check-diff-hygiene.sh

  secret-scan:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    env:
      BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || '' }}
      # The pull request's own head, not the ephemeral `refs/pull/N/merge`
      # commit `github.sha` names; see the diff-hygiene job for why.
      SCAN_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.event.merge_group.head_sha || github.sha }}
      # Labels exist only on pull_request; the footer override also covers merge_group.
      SECRET_SCAN_BYPASS: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'secret-scan-override') && '1' || '0' }}
      GITLEAKS_VERSION: 8.30.1
      GITLEAKS_LINUX_X64_SHA256: 551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb
    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          # Background auto-gc rewrites .git/shallow between the deepen fetch and
          # the ancestry check ("shallow file has changed since we read it").
          git config gc.auto 0
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch \
            --no-tags --prune --depth=1 origin "${SCAN_HEAD_SHA}"
          git checkout --detach --force "${SCAN_HEAD_SHA}"
          # Deepen both sides in lockstep. A boundary on one chain that the
          # other has already walked past is what leaks foreign commits into
          # the range, and `--unshallow` on an already-complete repository is a
          # fatal error rather than a no-op.
          unshallow_if_needed() {
            if [[ "$(git rev-parse --is-shallow-repository)" == "true" ]]; then
              git fetch --no-tags --unshallow origin
            fi
          }
          main_refspec='+refs/heads/main:refs/remotes/origin/main'
          if [[ -n "${BASE_SHA}" ]]; then
            git fetch --no-tags --depth=1 origin "${BASE_SHA}"
            depth=50
            until git merge-base "${BASE_SHA}" HEAD >/dev/null 2>&1; do
              if [[ ${depth} -gt 1600 ]]; then
                unshallow_if_needed
                break
              fi
              git fetch --no-tags --deepen="${depth}" origin "${SCAN_HEAD_SHA}" "${BASE_SHA}"
              depth=$((depth * 2))
            done
            git merge-base "${BASE_SHA}" HEAD >/dev/null
          else
            git fetch --no-tags --depth=1 origin "${main_refspec}"
            depth=50
            until git merge-base origin/main HEAD >/dev/null 2>&1; do
              if [[ ${depth} -gt 1600 ]]; then
                unshallow_if_needed
                break
              fi
              git fetch --no-tags --deepen="${depth}" origin "${SCAN_HEAD_SHA}" "${main_refspec}"
              depth=$((depth * 2))
            done
            git merge-base origin/main HEAD >/dev/null
          fi
          # A merge base existing is not the same as the whole shared ancestry
          # being connected. `--deepen` extends every shallow boundary, so a
          # branch that merged main in earlier reaches main through a second
          # chain rooted much further down; the loop stops at the first merge
          # base, and the main commits that chain exposes are never covered by
          # the base's own boundary, so the range does not exclude them. PR
          # #1226 head c5aad0ae6 carries ten such merges and reported 12
          # findings, all fingerprinted to 0625be3de, an ancestor of main.
          # With no merge in the range there is a single chain and the shallow
          # graph is already exact; with merges, only a complete graph excludes
          # correctly.
          if [[ -n "${BASE_SHA}" ]]; then
            range_base="$(git merge-base "${BASE_SHA}" HEAD)"
          else
            range_base="$(git merge-base origin/main HEAD)"
          fi
          if [[ "$(git rev-list --merges --count "${range_base}..HEAD")" -gt 0 ]]; then
            unshallow_if_needed
          fi
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0

      - name: Resolve secret scan range and override
        id: secret-scan
        run: |
          set -euo pipefail

          # The merge base, never `BASE_SHA` itself: on a pull request
          # `base.sha` is main's current tip and is normally not an ancestor of
          # the head, so `BASE_SHA..HEAD` would carry every main commit landed
          # since this branch was cut.
          if [[ -n "${BASE_SHA}" ]]; then
            if ! base_sha="$(git merge-base "${BASE_SHA}" HEAD)"; then
              echo "Secret scan could not resolve merge-base for '${BASE_SHA}'." >&2
              exit 1
            fi
          elif ! base_sha="$(git merge-base origin/main HEAD)"; then
            echo "Secret scan could not resolve merge-base for origin/main." >&2
            exit 1
          fi
          range="${base_sha}..HEAD"
          echo "base_sha=${base_sha}" >> "${GITHUB_OUTPUT}"

          if [[ "${SECRET_SCAN_BYPASS}" == "1" ]]; then
            echo "::warning::Secret scan bypassed by the secret-scan-override label."
            echo "bypass=1" >> "${GITHUB_OUTPUT}"
            exit 0
          fi

          commits_file="$(mktemp)"
          trailers_file="$(mktemp)"
          trap 'rm -f "${commits_file}" "${trailers_file}"' EXIT
          if ! git rev-list "${range}" > "${commits_file}"; then
            echo "Secret scan could not enumerate commits in range '${range}'." >&2
            exit 1
          fi

          footer_reason=""
          empty_footer_commits=""
          while IFS= read -r commit; do
            if ! git show -s --format=%B "${commit}" | git interpret-trailers --parse > "${trailers_file}"; then
              echo "Secret scan could not enumerate override footers for commit '${commit}'." >&2
              exit 1
            fi
            while IFS= read -r trailer; do
              [[ "${trailer}" == Bypass-Secret-Scan:* ]] || continue
              reason="${trailer#*:}"
              reason="${reason#"${reason%%[![:space:]]*}"}"
              reason="${reason%"${reason##*[![:space:]]}"}"
              if [[ -z "${reason}" ]]; then
                short_commit="$(git rev-parse --short "${commit}")"
                empty_footer_commits="${empty_footer_commits}${empty_footer_commits:+, }${short_commit}"
              elif [[ -z "${footer_reason}" ]]; then
                footer_reason="${reason}"
              fi
            done < "${trailers_file}"
          done < "${commits_file}"

          if [[ -n "${empty_footer_commits}" ]]; then
            echo "Secret-scan override-footer validation failed for commit(s) ${empty_footer_commits}: Bypass-Secret-Scan has an empty reason; supply a non-empty reason or use the secret-scan-override label." >&2
            exit 1
          fi
          if [[ -n "${footer_reason}" ]]; then
            echo "::warning::Secret scan bypassed by Bypass-Secret-Scan footer: ${footer_reason}"
            echo "bypass=1" >> "${GITHUB_OUTPUT}"
          else
            echo "bypass=0" >> "${GITHUB_OUTPUT}"
          fi

      - name: Download pinned Gitleaks binary
        if: steps.secret-scan.outputs.bypass != '1'
        run: |
          archive="${RUNNER_TEMP}/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz"
          curl --fail --location --silent --show-error \
            "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz" \
            --output "${archive}"
          echo "${GITLEAKS_LINUX_X64_SHA256}  ${archive}" | sha256sum --check
          tar -xzf "${archive}" -C "${RUNNER_TEMP}" gitleaks

      - name: Test secret rejection
        if: steps.secret-scan.outputs.bypass != '1'
        env:
          GITLEAKS_BINARY: ${{ runner.temp }}/gitleaks
        run: python3 scripts/test_ci_plan_hygiene.py

      - name: Scan pull request commit range
        if: steps.secret-scan.outputs.bypass != '1'
        env:
          SCAN_BASE_SHA: ${{ steps.secret-scan.outputs.base_sha }}
        run: |
          "${RUNNER_TEMP}/gitleaks" git --log-opts="${SCAN_BASE_SHA}..HEAD" --redact --verbose

  # This is the single required context after FIG-2055 lands. Keep every
  # correctness job in needs: the script rejects missing or unmapped entries,
  # failures/cancellations, ungated skips, and family skips the plan required.
  ci-conclusion:
    name: CI conclusion
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    if: always()
    needs:
      - plan
      - facade-only-examples
      - test-doc
      - repo-gates
      - unused-deps
      - lashlang-git-consumer
      - package-feature-checks
      - runtime-feature-boundary
      - workspace-tests
      - bazel-tests
      - heavy-tests
      - lint
      - diff-hygiene
      - secret-scan
      - unicode-tests
      - postgres-store
      - s3-store
      - fuzz-smoke
      - stack-budget
      - functional-e2e
      - functional-e2e-process-operations
      - worker-artifacts
      - restate-postgres-workers
      - restate-postgres-workers-summary

    steps:
      - name: Check out repository
        env:
          CHECKOUT_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth_header="$(printf 'x-access-token:%s' "${CHECKOUT_TOKEN}" | base64 -w 0)"
          export GIT_CONFIG_COUNT=1
          export GIT_CONFIG_KEY_0=http.extraheader
          export GIT_CONFIG_VALUE_0="AUTHORIZATION: basic ${auth_header}"
          git fetch --no-tags --prune --depth=1 origin "${GITHUB_SHA}"
          unset GIT_CONFIG_COUNT GIT_CONFIG_KEY_0 GIT_CONFIG_VALUE_0
          git checkout --detach --force FETCH_HEAD

      - name: Validate CI conclusion
        env:
          NEEDS_JSON: ${{ toJson(needs) }}
          BAZEL_TRUSTED: ${{ needs.plan.outputs.bazel_trusted }}
          WORKERS_E2E_ENABLED: ${{ github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.ref == 'refs/heads/main') || (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'ci:workers')) }}
        run: python3 scripts/ci/conclusion.py conclusion
